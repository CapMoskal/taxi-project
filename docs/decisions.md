# ADR-лог

Формат записи: дата, решение, почему, альтернативы (если были).

**Архив:** 9 записей от 2026-07-13 (скаффолд-этап — выбор MapLibre,
фейковая интерполяция маршрута, широкий охват, Vite 8→6, oxlint-биндинг,
Playwright-выбор, первая XState-фича, shadcn tsconfig-баг, дизайн-токены)
вынесены в [`_archive/decisions-scaffold.md`](_archive/decisions-scaffold.md).
12 записей 2026-07-15 — 2026-07-20 (MapTiler style + rAF-маркер, класс
такси/bottom-sheet, поиск водителя, авто-переходы поездки, оплата/рейтинг,
навигация, история поездок, PWA-полировка, реальная геолокация,
авто-подтверждение точки A, OSRM-маршрут, DestinationSheet) вынесены в
[`_archive/decisions-archive.md`](_archive/decisions-archive.md) при
компакте 2026-07-25 — обе фичи давно смёржены и устоялись, полная история
там (часть деталей позже пересмотрена более новыми записями ниже —
отмечено прямо в архиве). Ниже — записи 2026-07-23 и новее.

---

## 2026-07-23 — Камера следит за поездкой (throttled follow + override/автовозврат)

Референс — Яндекс.Такси: карта «следит» за поездкой, приближаясь по мере
подъезда, и не борется с пользователем, если он сам подвинул карту. Раньше
`MapCanvas` фитил `routeBounds` **один раз на ногу** (вся геометрия отрезка
целиком) — не следил за движением, не приближался, и на `selectingClass`
камера вообще не двигалась (A+Б не фитились).

- **Камера вынесена из `MapCanvas` в отдельный хук `useMapCameraFollow`**,
  вызывается из `OrderScreen`. `MapCanvas` перестал знать про `routeBounds`
  — единый источник управления камерой, не два конкурирующих (был бы риск,
  что `MapCanvas`'ный fit и хук дерутся). Throttled `fitBounds` (~раз в
  секунду, не на каждый rAF-кадр) — на статичном [A,Б] один фит, в
  движении переодически поджимает кадр по мере сближения точек (создаёт
  эффект приближения «на весь экран», без явного ручного управления zoom).
- **Кадр — «такси + следующая точка», не «такси + Б всегда»**: на
  `enRoute` — такси+A (куда машина реально едет), на `inRide` — такси+Б.
  Согласовано с Eugene явно (буквальная формулировка «такси+Б» была
  рассмотрена и отклонена — не показывает актуальную цель движения).
- **Override без кнопки recenter** — согласовано явно с Eugene. Только
  автовозврат по таймеру (4с бездействия). Гейт на `dragstart`/`zoomstart`/
  `rotatestart` через `e.originalEvent` — отличает жест пользователя от
  программного `fitBounds` (у которого `originalEvent` нет).
- **Реальный баг, найденный и исправленный при проверке (не гипотетический —
  подтверждён Playwright'ом с настоящим mouse-drag):** гейт на `originalEvent`
  ошибочно стоял и на `moveend` (по аналогии с `dragstart`). Оказалось,
  MapLibre после отпускания пальца/мыши применяет **инерционное
  доторможение** (`DragPanHandler`, инерция по накопленной скорости за
  последние ~160мс движения) — финальный `moveend`, которым это
  доторможение завершается, диспатчится **программно, без `originalEvent`**.
  С гейтом на `moveend` таймер автовозврата попросту никогда не
  планировался при быстром (флик-style) драге — слежение зависало в
  suppressed навсегда. Исправление: гейт на `moveend` убран, оставлена
  только проверка `suppressedRef` (сам флаг ставится только настоящим
  жестом через `dragstart`/`zoomstart`, так что момент включения
  по-прежнему защищён от программных фитов — только момент выключения не
  должен требовать `originalEvent`).
- **Тот же баг был и в уже смёрженном `DestinationSheet`** (фича bottom
  sheet, предыдущий пункт) — retreat-восстановление плашки после
  взаимодействия с картой гейтилось на `originalEvent` в `moveend` точно
  так же и так же ломалось бы на быстром флик-драге карты (плашка
  оставалась бы `retreated` навсегда). Пофикшено тем же способом в этом же
  коммите, а не отдельным — найдено по прямой аналогии при написании этой
  фичи, не отдельная сессия отладки.
- **Дев-хук `window.__map`** (`OrderScreen.tsx`, гейт `import.meta.env.DEV`):
  для Playwright-проверки камеры — чтение `map.getCenter()`/`getZoom()`
  напрямую надёжнее, чем реконструировать состояние камеры по экранным
  позициям маркеров (шумно — маркер такси сам двигается по ходу поездки,
  что маскирует, следит камера или нет). Не попадает в прод-сборку.
- **Padding по фазе**: `selectingClass`/`searchingDriver` (нижний шит) —
  больше `bottom`; `driverAssigned`/`enRoute`/`arrived`/`inRide`
  (`DriverCard` сверху) — больше `top`.
- Проверено (Playwright, реальная сеть OSRM): [A,Б] в кадре на
  `selectingClass`; слежение в движении — driver-маркер заметно двигается
  между сэмплами, кадр пересчитывается; ручной drag во время `inRide` не
  вызывает мгновенный snap-back (камера не борется с жестом); override +
  автовозврат подтверждён на статичной фазе (`selectingClass`) через
  polling `window.__map.getCenter()` — после драга камера смещается,
  держится смещённой ~1.5с, затем сама возвращается к исходному
  auto-fit-центру. Ноль консольных ошибок на всех прогонах.

- **2026-07-23 — Два бага пост-мёржа `camera-follow` (по скринам Eugene),
  оба в `DestinationSheet.tsx`:**
  1. **Прямая линия маршрута сразу после подтверждения Б.** Причина: до
     фикса `useGetRouteQuery({from: pickup, to: destination})` стартовал
     только в `OrderScreen`/`useRideAutomation` **после** `CONFIRM_DESTINATION`
     — реальный запрос к OSRM (не мок, задержка сети непредсказуема) ещё в
     полёте, пока рендерится `selectingClass`, `roadOrStraight` честно падает
     на прямую до ответа. Раньше это маскировалось (быстрый локальный OSRM
     при разработке), на реальной сети — заметная прямая линия. **Фикс**:
     прогрев кэша RTK Query — тот же `useGetRouteQuery` подписан ещё внутри
     `DestinationSheet`, пока плашка открыта, на `{from: pickup, to:
     candidateDestination}` (`candidateDestination` = то, что прочитает
     `handleConfirm`, обновляется на каждый `moveend`, включая программный
     `jumpTo` от выбора адреса — без гейта на `retreated`, в отличие от
     `draggedCenter`). Один и тот же сериализованный ключ аргументов → к
     моменту тапа «Подтвердить» запрос уже разрешён (или почти), `OrderScreen`
     переиспользует кэш вместо нового фетча. Проверено Playwright: список
     реальных запросов к `router.project-osrm.org` не содержит повторного
     запроса для пары pickup→destination после подтверждения — только тот,
     что стартовал во время открытой плашки; `route-line`-источник карты
     сразу после подтверждения содержит road-геометрию (десятки точек), не
     двухточечный fallback.
  2. **Кнопка «Подтвердить точку назначения» не видна без разворота
     плашки.** Причина: кнопка была последним ребёнком внутри свайпаемого
     `motion.div` (`h-[70vh]`, `translateY` по снапу). В дефолтном `peek`
     (`y = sheetHeight * 0.62`) видны только верхние ~38% плашки (шапка +
     инпут) — весь низ, включая кнопку, уезжал за нижний край экрана вместе
     с `translateY`. Не баг «в expanded списком перекрывает» — баг в том,
     что кнопка физически была частью уезжающего контента. **Фикс**:
     кнопка вынесена в отдельный `<div>`-сиблинг вне `motion.div`, `z-30`
     (плашка — `z-20`), `absolute inset-x-0 bottom-0` — не участвует в
     translateY снапа, видна всегда (`peek`/`expanded`/`retreated`).
     Список получил `pb-20`, чтобы последняя строка не пряталась под
     постоянным футером. Проверено Playwright: `boundingBox()` кнопки
     полностью внутри вьюпорта в дефолтном `peek`-состоянии без единого
     жеста.
  - Оба фикса — точечные, без нового пункта роадмапа (не архитектурная
    фича), по прямому запросу Eugene после ручной проверки на реальном
    устройстве/скринах.

- **2026-07-23 — Убрать «Начать заказ», `selectingPickup` = интерактивный
  первый экран (полная модель Яндекса, референс-скрин от Eugene):**
  - **Машина**: `initial: 'idle'` → `initial: 'selectingPickup'`, состояние
    `idle` и событие `START_ORDER` удалены целиком. `CANCEL_RIDE`/`RESET`
    ретаргетнуты с `idle` на `selectingPickup` — `resetOrder` очищает
    `pickup`, `PickupResolver` (гейт `pickup === null`) на новый заказ
    сеет геолокацию заново, поведение то же, что раньше давал `idle`.
  - **Развилка «сколько экранов»** — решили с Eugene: **два шага**, не
    один. `selectingPickup` — свой экран (двигаешь A, потом жмёшь «Куда
    едем?»), `selectingDestination` — отдельный (текущий
    `DestinationSheet`, не тронут функционально). Не стали схлопывать в
    один экран с сразу двумя точками — это отдельная, более крупная
    переделка `DestinationSheet`, не входила в запрос.
  - **`PickupResolver.tsx`** разделён на два: сам `PickupResolver` стал
    headless-сидером геолокации (убран авто-`CONFIRM_PICKUP` — раньше он
    проскакивал `selectingPickup` сразу же, что было ровно тем поведением,
    которое теперь отменяем), весь UI выехал в новый `PickupSheet.tsx`.
  - **`PickupSheet.tsx`**: центр-пин = точка A, `moveend` → `SET_PICKUP` —
    тот же паттерн, что `DestinationSheet` уже использует для Б. Два бага
    найдены и пофикшены в процессе (Playwright, реальная геолокация):
    1. **Список без капа по высоте закрывал центр-пин.** `BottomSheet` без
       ограничения высоты растягивался под 5 моковых недавних адресов
       почти до середины экрана — центр-пин (= истинный `map.getCenter()`,
       та же координата, что уйдёт в `pickup`) визуально прятался под
       шитом, хотя технически всё работало верно (WYSIWYG ломался только
       зрительно). Фикс: `max-h-[45vh] flex flex-col` на `BottomSheet` +
       `min-h-0 flex-1 overflow-y-auto` на список (тот же приём, что
       `DestinationSheet.tsx` уже использует для своего списка).
    2. **`jumpTo` при выборе недавнего адреса корраптил `pickup`.**
       `handlePickRecent` дергает `map.jumpTo()` на координаты Б и тут же
       шлёт `CONFIRM_PICKUP` — но `moveend`-listener (ещё смонтирован в
       момент вызова `jumpTo`, React не размонтировал `PickupSheet`
       синхронно) принимал этот программный прыжок за «пользователь
       подвинул A» и переписывал `pickup` координатами Б через
       `SET_PICKUP`. Итог — `pickup === destination`, OSRM на
       `selectingClass` возвращал вырожденный 2-точечный маршрут
       (route/v1/driving/lng,lat;**тот же** lng,lat) — прямая линия вместо
       дорог, обнаружено именно так (`from`/`to` совпали в URL запроса).
       Фикс: `isLeavingRef` — ставится в `true` перед `jumpTo` в
       `handlePickRecent`, `moveend`-хендлер игнорирует `SET_PICKUP`, пока
       флаг взведён (экран всё равно размонтируется). Не гейт на
       `e.originalEvent` (как в `DestinationSheet`/`useMapCameraFollow`) —
       здесь после `jumpTo` НЕ нужно вообще никакой реакции на `moveend`
       (в отличие от той пары мест, где программный `moveend` — легитимный
       сигнал типа «камера домчала», просто без пользовательского жеста).
  - **Тап по недавнему адресу на `selectingPickup`** ведёт на
    `selectingDestination` с центр-пином Б уже на этом адресе (не сразу в
    `selectingClass`) — подтверждено с Eugene явно (два кандидата
    обсуждались: «сразу в класс» и «на экран Б с шансом поправить»,
    выбран второй, ближе к реальному Яндексу).
  - **`ProfileButton.tsx`** — аватар вынесен из удалённого `IdleOverlay` в
    отдельный компонент `screens/order/`, рендерится на `selectingPickup`
    рядом с `PickupSheet`. Пилюля адреса `PickupSheet` — `left-20` (не
    `inset-x-4`), чтобы не наезжать на аватар в top-left.
  - **`PlaceRow.tsx`** — общая презентационная строка адреса, вынесена из
    `DestinationSheet.tsx` (был инлайн `<button>` с Clock/MapPin) в
    `features/order-flow/`, переиспользуется в `PickupSheet.tsx` — чистый
    рефактор без изменения поведения `DestinationSheet`.
  - Проверено Playwright (мобильный вьюпорт, реальная сеть — GPS-мок,
    OSRM, MapTiler): загрузка без «Начать заказ» → сразу интерактивный
    экран A; драг двигает A и обновляет пилюлю; «Куда едем?» → экран Б;
    тап по недавнему на экране A → экран Б на адресе, кнопка «Подтвердить»
    видна; сквозной smoke A→Б→класс — road-геометрия (143 точки, не 2);
    `CANCEL_RIDE` из `searchingDriver` возвращает на рабочий экран A;
    аватар → профиль. Ноль консольных ошибок на всех прогонах.

- **2026-07-23 — Демо для Миши: хостинг на Vercel + MSW включается и в
  prod.** Обсуждали два пути показать демо: локальный запуск у Миши
  (`npm run dev`/`preview`) vs хостинг. Eugene выбрал хостинг — ключевая
  причина: Миша должен видеть **последующие** правки по мере того, как мы
  их вносим, без ручной синхронизации/перезапуска на его стороне. Vercel
  (а не Netlify/GH Pages) — не принципиально, но: zero-config для Vite,
  автодеплой на каждый пуш в GitHub `main` закрывает «видит новые правки»
  само по себе, https из коробки (нужен геолокации — secure context), и
  никакого гемора с sub-path/service-worker scope, который был бы на
  GitHub Pages (прод-сборка там на `/repo-name/`, а не в корне).
  **Разобранный нюанс, который чуть не завёл не туда:** MSW — единственный
  «бэкенд» проекта, но исторически гейтился на `import.meta.env.PROD`
  (см. `.claude/rules/msw-mocking.md`, на момент этой записи ещё не
  обновлён) — значит обычная прод-сборка (то, что уедет на любой
  хостинг) отдаёт красивую оболочку с полностью мёртвыми запросами.
  Разбирали три варианта: (а) хостить `dev` — не для этого предназначен,
  не вариант для публичного хостинга; (б) хостить `preview` — не решает
  проблему, `vite preview` раздаёт ТУ ЖЕ прод-сборку, MSW там тоже
  выключен; (в) снять `PROD`-гейт, MSW работает и в prod. Выбрали (в) —
  единственный вариант, при котором задеплоенная сборка работает
  полностью, как и было задумано изначально («нет бэкенда = MSW
  имитирует», см. `CLAUDE.md`) — просто теперь это верно в любой среде,
  не только в dev. **Дальше по плану** (см. `roadmap.md`): снять гейт в
  `main.tsx`, обновить `.claude/rules/msw-mocking.md` (сейчас там написано
  «MSW включается только в dev» — как только гейт снят, это устареет, не
  забыть поправить в этом же коммите/фиче, не оставлять расхождение
  между кодом и правилами), завести проект на Vercel с автодеплоем
  `main`, проверить PWA-установку и геолокацию с реального телефона.

- **2026-07-23 — Реализация деплой-фичи: MSW-first, Workbox-SW не
  регистрируется.** По ходу реализации всплыла развилка, которой не было
  в обсуждении выше: **MSW и Workbox-PWA-SW не могут работать вместе** —
  оба претендуют на scope `/`, страницу контролирует только один service
  worker. До сих пор они не пересекались случайно (MSW — только dev,
  PWA-SW — только прод); сняв гейт, мы столкнули их лбами. Решение
  Eugene: **MSW важнее** (рабочие данные — суть демо), Workbox-SW — в
  жертву. Реализация:
  - `main.tsx`: гейт `if (import.meta.env.PROD) return` удалён — MSW
    стартует всегда (dynamic import остался, `bypass` остался).
  - `vite.config.ts`: `injectRegister: false` — Workbox-SW и манифест
    по-прежнему собираются, но регистрация SW в приложение не инжектится
    (`registerSW.js` больше не генерится, `sw.js` лежит в `dist/`
    инертным). Манифест-линк и `theme-color` в `index.html` остаются —
    установка «на экран Домой» работает. Потеряли: precache оболочки и
    офлайн-кэш тайлов MapTiler. `OfflineBanner` жив (он от
    `navigator.onLine`, не от SW). Если появится реальный бэкенд — просто
    вернуть `injectRegister`.
  - `vercel.json`: SPA-rewrite `/(.*)` → `/index.html` (реальные файлы
    Vercel отдаёт напрямую, rewrite ловит только несуществующие пути).
  - Ручные шаги на стороне Eugene (не автоматизируемы — его аккаунты):
    импорт репо в Vercel + env-переменная `VITE_MAPTILER_KEY` (ключ
    гитигнорится в `.env`, без него карта не загрузится) + проверка с
    реального телефона.
  - Проверено локально (Playwright на `npm run preview` — та же
    прод-сборка, что уедет на Vercel; свежий браузер-контекст, чтобы
    старый Workbox-SW с прошлых локальных preview не влиял): MSW
    перехватывает `/api/*` (профиль «Евгений», классы с ценами, недавние
    адреса — всё рендерится, в консоли `[MSW] 200 OK`);
    `navigator.serviceWorker.getRegistrations()` → ровно одна
    регистрация, `mockServiceWorker.js` на scope `/`; сквозной путь
    А → Б → классы с road-маршрутом; ноль ошибок в консоли.
  - **Грабля на первом же деплое: `EBADPLATFORM`.** Установка зависимостей
    на Vercel (Linux x64) падала из-за `@oxlint/binding-darwin-arm64` —
    явного macOS-arm64 devDependency, добавленного 2026-07-13 как
    осознанный костыль под локальный Node 22.2.0 (см.
    `_archive/decisions-scaffold.md` — там же было предсказано, что при
    появлении Linux-окружения запись придётся пересматривать). Прямой dep
    под чужую платформу — жёсткий фейл npm; убрать нельзя (локальный lint
    ломается — проверено на свежем клоне), в `optionalDependencies`
    нельзя (npm молча пропускает его из-за того же engine-mismatch — тоже
    проверено, исходная проблема костыля). Решение:
    `"installCommand": "npm install --force"` в `vercel.json` — `--force`
    обходит платформенную проверку (проверено зеркальным экспериментом:
    linux-биндинг на macOS с `--force` ставится, lint работает); oxlint
    на Vercel всё равно не запускается (build = `tsc -b && vite build`).
    Чистое снятие костыля = обновить локальный Node до `>=22.12` — тогда
    и явный биндинг, и `--force` можно убрать (отдельной задачей).

- **2026-07-25 — Коммитируемая регресс-сеть на `@playwright/test`,
  пересмотр решения от 2026-07-13.** Изначально (`_archive/decisions-scaffold.md`,
  2026-07-13) решили НЕ коммитить переиспользуемый Playwright-раннер —
  тогда приложение было скаффолдом, разовых Node-скриптов хватало. К
  2026-07-25 приложение выросло настолько, что ручная перепроверка всех
  флоу руками в каждой сессии перестала масштабироваться — Eugene
  запросил регресс-сеть явно, обсудив альтернативы (см. ниже «отложенный
  пункт»). Развилки, решённые с Eugene:
  - **Гонять `npm run test:e2e` перед КАЖДЫМ коммитом**, наравне с
    build+lint (не только перед мёржем) — обязывает набор быть быстрым
    и детерминированным.
  - **Внешние API (OSRM, MapTiler) мокаются**, не реальная сеть — ради
    скорости/стабильности гейта-на-коммит; реальную интеграцию по-прежнему
    проверяем ручным прогоном по живому URL (как и раньше).
  - Мои технические решения (обоснованы фактами, не форки с Eugene):
    - **Цель тестов — `npm run dev`, не `preview`.** MSW одинаково себя
      ведёт в dev/preview/prod (см. запись выше про Vercel), но дев-хук
      `window.__map` (`OrderScreen.tsx`) существует только при
      `import.meta.env.DEV` — тестам он нужен для проверки геометрии
      маршрута и камеры.
    - **Внешние API мокаются через MSW, не `page.route()`.** Первая
      попытка — мокать OSRM/MapTiler через `page.route()` в тестовом
      файле — не сработала: запросы были видны в `page.on('request')`,
      но обработчик `route()` ни разу не сработал. Причина — активный
      Service Worker (MSW): fetch, который SW пропускает дальше в сеть
      (`onUnhandledRequest: 'bypass'`), выполняется в собственном
      контексте SW, отдельном CDP-таргете от страницы — `page.route()`
      этого не видит (подтверждено экспериментом: добавил
      `console.log` в обработчик route — ни разу не вызвался, при этом
      сами запросы шли и долетали до реального OSRM/MapTiler). Раз MSW
      и так уже единственный слой перехвата в приложении — мокаем через
      него же: `src/shared/mocks/e2e-handlers.ts` с хендлерами для OSRM/
      MapTiler (geocoding + style), подключаются в `browser.ts` только
      когда `import.meta.env.VITE_E2E` — переменную выставляет
      исключительно `webServer.env` в `playwright.config.ts`; обычный
      `npm run dev` и прод (Vercel) её не видят, там OSRM/MapTiler
      остаются настоящими сервисами, как и задумано изначально. Мок-стиль
      карты — `{version:8, sources:{}, layers:[]}` (валидный, карта
      грузится мгновенно офлайн; MapLibre не запрашивает тайлы для
      несуществующих источников — отдельно мокать тайлы не пришлось).
    - **Собственные `/api/*` не мокаются повторно** — тесты используют
      тот же MSW-бэкенд, что и обычная разработка. `driver-search` имеет
      встроенную 20%-ю случайность отказа (`entities/driver/mocks.ts`) —
      обрабатывается ретраем в самом тесте (клик «Повторить поиск» в
      цикле до появления `DriverCard`), не отдельным мокoм — заодно
      покрывает retry-UI.
    - **`page.clock` для перемотки времени поездки.** Полный флоу
      завязан на реальные задержки: `driverAssigned→enRoute` (2с,
      `setTimeout` в `useRideAutomation.ts`) и две 6-секундные rAF-
      анимированные ноги (`useAnimatedPosition.ts`, время читается через
      `performance.now()`). Без перемотки — 15+ реальных секунд на
      каждый прогон, неприемлемо для гейта-на-коммит.
      `page.clock.install()` + `fastForward()` эмпирически подтверждён
      консистентно двигающим и `setTimeout`, и rAF/`performance.now()`
      вместе (мини-эксперимент до написания финального теста: статус
      карточки водителя корректно переходил «Едет к вам» →
      «Ждёт у подъезда» после одного `fastForward(6200)`). MSW-шные
      искусственные задержки (`delay()` внутри Service Worker) фейком не
      затрагиваются — не тот JS-контекст — остаются короткими честными
      ожиданиями (~0.5–2.5с), это и ограничивает нижнюю границу времени
      прогона (не 15с, но и не мгновенно).
  - **Санити-проверка**: временно сломал `roadOrStraight()` (вернуть
    всегда прямую линию) — тест `pickup-destination.spec.ts` покраснел
    ровно на той самой проверке, что и должен (`coords.length > 2`);
    откатил. Подтверждает, что набор реально ловит регресс, а не просто
    зелёный по случайности.
  - Набор: `e2e/smoke.spec.ts`, `e2e/pickup-destination.spec.ts`,
    `e2e/full-ride.spec.ts` (полный happy-path A→оплата/рейтинг→RESET).
    Весь прогон — секунды (~10с локально), не десятки.
  - **Флейк в `full-ride.spec.ts`, найден и пофикшен в тот же день.**
    После первого коммита набора Eugene попросил ещё и CI-бэкстоп (ниже) —
    при проверке в чистом окружении (свежий `git clone`-эквивалент,
    `npm install --force`, без прогретого `node_modules/.vite`)
    `full-ride.spec.ts` иногда падал на `expect(errors).toEqual([])`.
    **Первая гипотеза оказалась неверной**: заподозрил гонку параллельных
    воркеров с холодным кэшем Vite (по 4 браузера одновременно бьют в
    только что стартовавший dev-сервер) — временно поставил `workers: 1`
    для CI. Прогнал 12 раз подряд локально с `--workers=1` — всё зелёное,
    гипотеза выглядела подтверждённой. Но при повторной проверке уже
    ОБЫЧНОГО (не чистого) окружения тест всё равно иногда падал даже в
    одиночном прогоне без параллелизма — гипотеза не выдержала. Добавил
    прицельное логирование (`page.on('response')`/`page.on('requestfailed')`)
    и погонял ~12 раз — поймал настоящую причину: `entities/driver/mocks.ts`
    имеет честную `SEARCH_FAILURE_RATE=0.2` на `/api/driver-search` — при
    срабатывании MSW отвечает 404 (это штатное поведение мока, тест уже
    обрабатывает это ретраем «Повторить поиск»), но Chrome ЛЮБОЙ non-2xx
    ответ логирует в консоль как «Failed to load resource» автоматически,
    независимо от того, обработало ли приложение это штатно. `errors`-массив
    в тесте ловил и это тоже. Убрал спекулятивный `workers: 1` (не был
    реальной причиной), пофиксил по существу: `full-ride.spec.ts`
    фильтрует такие сообщения по `msg.location().url.includes('/api/driver-search')`
    — **не по `msg.text()`**, там просто общая строка без URL, ловушка на
    будущее для похожих фильтров. Подтверждено статистически: 22 подряд
    зелёных прогона после фикса (шанс так повезти при живом баге с 20%-й
    частотой — около 0.5%).
  - **CI-бэкстоп** (`.github/workflows/ci.yml`, запрошено Eugene «на
    всякий случай»): гоняется на каждый пуш в `main` (не PR-гейт — в
    проекте GitHub PR не используются, фича-ветки только локальные,
    видел `git branch -a`/`git remote -v` перед тем как решать триггер).
    `build` → `lint` → `test:e2e`, Node 22, `npm install --force` (тот же
    обход `EBADPLATFORM`, что в `vercel.json`, см. запись 2026-07-23),
    `playwright install --with-deps chromium`, при падении —
    `playwright-report` в артефакт. Собственно ловля флейка выше
    случилась именно при подготовке к этому CI (тестировал в чистом
    окружении, чтобы предсказать поведение CI перед тем как коммитить).
  - **Отложенный пункт (явная просьба Eugene не терять): Playwright
    MCP.** Обсуждали как альтернативу/дополнение — таблица
    плюсов/минусов в переписке 2026-07-25. Решили: НЕ заменяет ни
    регресс-сеть (эта запись), ни разовые Node-скрипты для глубокой
    отладки (кастомные ассерты, `page.evaluate` в живое состояние
    приложения — то, чем реально ловились баги весь проект) — MCP дал
    бы только быстрый интерактивный осмотр в диалоге без написания
    скрипта на лету. Пункт сознательно не реализован сейчас, вернуться
    отдельной задачей, когда понадобится ускорить именно ad-hoc
    тыканье — см. `docs/roadmap.md`.

## 2026-07-25 — Наполнение профиля: подстраницы по референсу Яндекс.Такси (не клон)

Миша попросил больше «наполненности» в приложении. Eugene прислал скрин
профиля Яндекс.Такси как референс — обсудили, что из него органично для
витрины такси на этом этапе, а что нет.

- **Взяли:** ряд из 4 иконок-кнопок под аватаром (Заказы/Поддержка/Адреса/
  Настройки — даёт ощущение «дорого» почти бесплатно), полная страница
  «Способы оплаты» (список + честная MSW-мутация «добавить карту»),
  «Адреса» (Дом/Работа), «Настройки» (реальный переключатель темы +
  тумблер уведомлений), «Информация» (статика), «Поддержка» (FAQ-заглушка).
- **Явно НЕ взяли** (обсуждено с Eugene): «Избранное» (пересекается по
  смыслу с «Адресами», дублирование без пользы), «Кешбэк и скидки»/«Сейф»/
  «Помогать с Яндексом» (геймификация конкретно Яндекс.Плюс/эко-проекта —
  проект прямо не клон Яндекса, копировать их фирменную механику
  бессмысленно), «Бизнес-аккаунт»/«Семейный аккаунт» (мультиаккаунтность —
  отдельная модель данных, весь остальной флоу заказа её не поддерживает,
  несоразмерно ради косметики).
- **`paymentMethod` вынесен из `UserProfile` в отдельную entity
  `entities/payment-method/`** — единый источник истины (список карт +
  наличные, одна с `isDefault`). Проверено перед миграцией: поле читалось
  только в `ProfileScreen`, не в шаге оплаты поездки (`RideCompletionSheet`)
  — миграция безопасна для флоу заказа.
- **Новая entity `entities/saved-place/`** (Дом/Работа), отдельно от
  `entities/recent-place/` — разная семантика (помеченные пользователем
  адреса vs авто-собираемые недавние точки), не смешивать в один эндпоинт.
- **«Добавить карту» — честная MSW-мутация**, не заглушка: `POST
  /api/payment-methods` дописывает карту в module-scoped массив внутри
  хендлера, RTK `addCard` с `invalidatesTags` перечитывает список. В духе
  правила проекта «моки имитируют поведение честно», не просто рисуют
  статичный UI.
- **Профильные подстраницы — без рандомной инъекции ошибок.** Правило
  «ошибочные ответы иногда» уже реализовано на `driver-search`
  (единственный намеренный флейк, см. запись выше про регресс-сеть) —
  здесь это была бы просто порча демо без пользы, не стали повторять.
- **Переключатель темы — настоящий, не бутафория.** `.dark`-токены в
  `index.css` были определены с самого начала (design-tokens.md,
  2026-07-13), но ничего в приложении их не включало — тема всегда была
  светлой. Завели `app/ThemeProvider` (состояние `light|dark|system`,
  класс `.dark` на `document.documentElement`, персист в `localStorage`,
  `system` слушает `matchMedia`) — по образцу уже существующего
  `NavigationProvider`. Это UI-состояние приложения, не серверные
  (мокнутые) данные и не флоу заказа — сознательно НЕ через RTK Query и
  не через XState (правило проекта: RTK — источник истины для мокнутых
  данных, XState — для флоу заказа, тема ни то ни другое).
- **Навигация — плоский `Screen`-union расширен на 5 значений**, без
  стека: дерево мелкое (order → profile → подстраница), «назад» у всех
  подстраниц ведёт в profile — тот же паттерн, что уже был у `history`.
  Стек посчитали оверинжинирингом для текущей глубины навигации.
- Заодно вынесен `shared/ui/ScreenHeader` (был продублирован в
  `ProfileScreen`/`RideHistoryScreen`) и добавлены переиспользуемые
  `ListRow`/`Switch` — общие для всех новых подстраниц.
- Регресс-сеть: `e2e/profile.spec.ts` — навигация по всем подстраницам без
  console-ошибок, добавление карты, переключение темы (assert на класс
  `.dark`). При ручной проверке (`npm run dev`, мобильный вьюпорт) нашли и
  сразу поправили мелкий баг форматирования — список карт показывал
  «•• 0542» (2 точки) вместо принятого везде «•••• 0542» (4 точки).

## 2026-07-25 — Карта следует за темой приложения (light/dark)

Eugene спросил, можно ли перекрасить карту вместе с темой (после того как
случайно добавил тестовую карту в Способах оплаты и заодно попросил тёмную
карту). MapTiler-стиль, маршрут и маркеры теперь переключаются вместе с
`.dark`.

- **Тёмный стиль — `basic-v2-dark`**, точная тёмная пара к уже
  используемому светлому `basic-v2`. Не гадал — проверил напрямую реальным
  ключом (`curl` с `VITE_MAPTILER_KEY` из `.env`): `basic-v2-dark` → 200,
  заведомо несуществующий slug → 404 (200 у всех slug'ов без ключа —
  MapTiler проверяет ключ раньше, чем slug, так что «просто curl без
  ключа» ничего не доказывает, это тоже проверено).
- **`ThemeProvider` теперь отдаёт `resolvedTheme: 'light' | 'dark'`**
  (`themeContext.ts`) — разрешённое значение с учётом режима `system`
  (matchMedia), реактивно меняется и при live-флипе темы ОС. `mode`
  (`light|dark|system`, для UI Настроек) и `resolvedTheme` (для
  потребителей вроде карты) — разные вещи, не путать.
- **Карта переключается через `map.setStyle()`, не пересоздание.**
  DOM-маркеры (`maplibregl.Marker`) не часть GL-стиля — переживают
  `setStyle()` сами, ничего специально переприкреплять не нужно.
  Route-line — часть стиля, `setStyle()` его стирает вместе со старым
  стилем, значит после смены стиля его нужно переналожить.
- **Цвет route-line ре-резолвится в момент переналожения**, не кэшируется
  между переключениями — иначе после смены темы линия осталась бы цветом
  старой (резолвится из `var(--foreground)` через `resolveCssColor()`,
  которая уже существовала для линии маршрута; см. запись 2026-07-15 в
  архиве про сам механизм резолва oklch-переменных для WebGL-`paint`).
- **Найденный и исправленный баг (ручная проверка, не e2e):** `MapCanvas`
  сначала проверял `map.isStyleLoaded()` сразу после своего же
  `map.setStyle()` — и получал устаревший `true` от ещё не до конца
  замененного старого стиля. Из-за этого переналожение route-line
  срабатывало «немедленно» на исчезающий старый стиль: слой на секунду
  появлялся, а затем реальная асинхронная замена стиля стирала его
  без остатка, потому что слушатель `styledata` в этом случае не
  регистрировался вовсе. Поймано только ad-hoc-скриптом с прицельным
  логированием (`page.on('console')` + опрос `map.getLayer()`/
  `isStyleLoaded()` в цикле) — без него баг был не виден, приложение не
  падало и не логировало ошибок, просто маршрут молча пропадал через
  секунду после смены темы. Исправлено на постоянный слушатель
  `styledata`, который перепроверяет `isStyleLoaded()` внутри себя и
  отписывается сам, когда стиль действительно готов — `styledata`
  срабатывает несколько раз за одну смену стиля, и не каждое срабатывание
  означает готовность именно нового стиля.
- **Без отдельного e2e-теста на сам свап.** Под `e2e-handlers.ts` оба
  стиля (`basic-v2` и `basic-v2-dark`) мокаются одним и тем же
  `EMPTY_STYLE` — различить их ассертом нельзя, а живого UI-пути
  переключить тему прямо на экране заказа нет (`ProfileButton` есть
  только в `selectingPickup`). Добавлен мок `basic-v2-dark` → `EMPTY_STYLE`
  в `e2e-handlers.ts` (дёшево и правильно на будущее — иначе реальный
  прогон под `VITE_E2E` ушёл бы во внешний MapTiler при тёмной теме),
  но сама проверка смены — ручная (ad-hoc-скрипт с `page.emulateMedia()`
  + `page.clock`-independent опрос, как описано выше), не в
  `test:e2e`. Существующие 9 e2e-тестов не тронуты и остаются зелёными.

## 2026-07-27 — Десктоп-эпик разбит на 2a/2b/2c, реализован фундамент (2a)

Eugene прислал скрин реального `taxi.yandex.ru` (десктоп) и хочет повторить
этот UI/UX-паттерн: панель заказа «всё сразу» (адреса+класс+оплата+
«Заказать» co-visible, не пошагово), охват заказ+профиль/история, полный
навбар. Явно попросил двигаться инкрементами, не одной гигантской фичей —
разбил на **2a** (фундамент, этот коммит), **2b** (двухколоночный каркас
заказа, флоу остаётся пошаговым), **2c** («всё сразу» + переосмысление
чтения XState-снапшота в `OrderScreen`, самое рискованное — последним).
Полное текстовое описание референс-скрина — `docs/roadmap.md` (сам PNG не
сохранён в репозиторий, прислан инлайн в чат).

**2a — что сделано:**
- **Брейкпоинт `lg` (1024px) — первый responsive-слой в проекте.** До этого
  коммита в `src/` не было ни одного `md:`/`lg:`/`@media` (грепнул перед
  стартом) — всё позиционировано абсолютно и хардкодит `h-dvh`. `lg`
  выбран как стандартный Tailwind desktop-брейкпоинт, ничего специфичного
  под этот проект не требуется.
- **Рефактор `h-dvh` → `h-full` на всех экранах + навбар в корне.** Навбар
  сверху требует, чтобы корень (`app/App.tsx`) сам владел `h-dvh flex-col`,
  а не каждый экран отдельно — иначе навбар+экран вместе превысили бы
  вьюпорт. `DesktopNavbar` — `shrink-0`, область экрана — `flex-1 min-h-0`.
  Экраны теперь просто заполняют выданное место (весь вьюпорт на мобильном,
  под навбаром на десктопе) — публичное поведение не изменилось.
- **`DesktopNavbar` (`screens/_layout/`)** — наш вордмарк «Такси» (НЕ лого
  Яндекса — проект прямо не клон Яндекса, см. `CLAUDE.md`), текстовые
  пункты-заглушки
  (Пассажирам/Водителям/Бизнесу/Помощь — инертные, без `navigate`, это
  корп-сайт-хром референса, не функциональность приложения), аватар →
  профиль. Заменяет плавающий `ProfileButton` на экране заказа: тот
  получил `lg:hidden`, а не удалён — так мобильное поведение остаётся
  буквально нетронутым при любой ширине ниже брейкпоинта.
- **`shared/ui/ScreenShell`** — общий `ScreenHeader` + центрированная
  колонка (`lg:max-w-2xl`), заменил идентичную обёртку `flex h-dvh w-full
  flex-col bg-background` + `flex-1 overflow-y-auto p-4`, продублированную
  в 7 экранах (Profile/History/PaymentMethods/Addresses/Settings/Info/
  Support) — заодно закрыл дублирование, не только под десктоп.
  `relative` — на скролл-контейнере, а не на центрированной внутренней
  колонке, чтобы `absolute`-оверлеи вроде `AddCardSheet` в
  `PaymentMethodsScreen` анкорились на всю ширину панели, а не на
  `max-w-2xl` колонку (как `BottomSheet` уже ведёт себя на мобильном —
  край в край, не по центру).
- **Экран заказа тронут МИНИМАЛЬНО — полноценный каркас в 2b.** `BottomSheet`
  (shared-примитив, используется и в order-flow, и в `AddCardSheet`) и
  `pickup-pill` на `lg:` капаются по ширине и докуются в угол — интерим,
  чтобы 2a не выглядел сломанным на широком экране, явно помечено
  комментарием в коде как временное решение до 2b.
- Регресс-сеть: `e2e/desktop-layout.spec.ts` (новый вьюпорт 1280×800 через
  `test.use`) — навбар виден+кликабелен только на десктопе, контентная
  колонка реально центрирована (замер `boundingBox`, не просто наличие
  класса), плавающий `ProfileButton` скрыт на десктопе. Мобильный сьют
  (13 тестов) — без изменений, `lg:`-классы там неактивны.
- **`CLAUDE.md`/`.claude/rules/testing.md` обновлены ДО реализации** (по
  просьбе Eugene) — десктоп больше не «бонус на потом» для проверки
  глазами, фичи с десктоп-вёрсткой проверяются в обоих вьюпортах.

## 2026-07-27 — Десктоп 2b: двухколоночный каркас экрана заказа

Второй кусок десктоп-эпика: навбар (2a) + **левый рельс 380px** с UI
текущей фазы + **карта на всё остальное**. Флоу остаётся ПОШАГОВЫМ, как на
мобильном — меняется только КУДА монтируется UI фазы, не сама модель
состояний. «Всё сразу» — 2c, ещё впереди.

- **`useIsDesktop()` (`shared/lib/`) — JS, не только CSS.** Реактивный
  `matchMedia('(min-width: 1024px)')`, синхронно с Tailwind `lg`. Нужен
  именно JS: фаза-компоненты должны физически монтироваться в РАЗНОЕ место
  дерева (рельс vs оверлей над картой) — чистый CSS-подход 2a (просто
  скрыть/показать один и тот же DOM) сюда не масштабируется, плюс
  `DestinationSheet` должен полностью отключать drag-физику на десктопе
  (не просто перекрасить), а это тоже не CSS-задача.
- **Каждый фаза-компонент монтируется РОВНО в одном месте одновременно**
  (`isDesktop && <PickupSheet/>` в `<aside>` **или** `!isDesktop &&
  <PickupSheet/>` в области карты — никогда оба сразу). Это не
  косметика: у большинства этих компонентов есть побочные эффекты
  (map-листенеры `moveend`/`dragstart`, `actorRef.send(...)`) — смонтируй
  их дважды, и оба инстанса независимо подписались бы на карту и
  задваивали дispatch на каждое движение. `PickupResolver` (headless
  геолокация) и `ProfileButton` вынесены ИЗ этой развилки — резолвер
  обязан работать независимо от брейкпоинта (иначе на десктопе `pickup`
  никогда бы не проставился), `ProfileButton` уже сам умеет `lg:hidden`
  с 2a, незачем дублировать это решение в двух местах.
- **`OrderSurface` (`shared/ui/`) — sheet↔panel** для 5 «простых» фаз
  (список в Pickup, ClassPicker, DriverSearch, Completion, Done):
  мобильный путь рендерит `BottomSheet` как раньше (байт в байт то же
  поведение), десктоп — плоский `<div>` без motion/drag/absolute (это
  теперь обычный ребёнок `<aside>` в document flow, а не оверлей).
- **`PickupSheet`/`DriverCard` — bespoke-форк вручную**, не через
  `OrderSurface` (у обоих есть элемент, которого при монтировании в рельс
  просто не должно быть — плавающая pill-плашка адреса и `absolute
  top-0`-карточка водителя соответственно). На десктопе pill/карточка
  становятся обычной первой строкой рельс-блока.
- **`DestinationSheet` — самый рискованный форк, обошлось без потери
  функциональности.** Разделил на два независимых слоя, которые раньше
  были слиты в одной переменной: (1) ВИЗУАЛЬНАЯ физика шторки
  (`snap`: peek/expanded/retreated, drag, retreat-при-движении-карты) —
  теперь строго мобильная, на десктопе не создаётся вовсе (статичный блок,
  ретритить нечему); (2) ЛОГИКА синка адреса при движении карты (drag
  карты → `candidateDestination`/`draggedCenter` → reverse-geocode →
  обновление текста инпута, plus route-prefetch) — общая для обеих веток,
  **на десктопе не убрана**, интеракция выбора A/Б там такая же, как на
  мобильном (переход на «поля адреса как основной ввод» — это 2c, не
  трогаем сейчас). Раньше признаком «это был реальный драг юзера, не наш
  jumpTo» служило значение `snap === 'retreated'` — но раз на десктопе
  этого стейта больше нет, завёл отдельный `isUserDrivenMoveRef`,
  независимый от визуального snap, которым размечено и мобильное, и
  десктопное поведение одинаково.
- **`map.resize()` на пересечение брейкпоинта.** MapLibre's `trackResize`
  слушает только `window`-ресайз, не изменение размера СВОЕГО контейнера —
  а рельс появляется/исчезает без изменения размера окна (флекс просто
  перераспределяет ширину). Без явного `map.resize()` карта продолжала бы
  рендериться в устаревшем размере после пересечения `lg`. Плюс отдельный
  паддинг камеры для десктопа (`{40,40,40,40}` вместо
  `BOTTOM_SHEET_PADDING`/`DRIVER_CARD_PADDING`) — на рельсе нет оверлеев
  поверх карты, которые раньше оправдывали неравномерные отступы.
- **`BottomSheet.tsx`'s `lg:`-докинг из 2a теперь не мёртвый код, но
  единственный настоящий потребитель — `AddCardSheet`** (профиль,
  `PaymentMethodsScreen`). Order-flow больше не доходит до `BottomSheet` на
  десктопе вообще — `OrderSurface` форкает раньше него. Обновил
  поясняющий комментарий в файле, чтобы это не выглядело как забытый
  дохлый интерим.
- **Найден и исправлен реальный баг в `playwright.config.ts`** (см.
  отдельный коммит того же дня) — `projects[].use: {
  ...devices['Desktop Chrome'] }` содержит собственный `viewport:
  {1280,720}`, который молча перекрывал глобальный mobile-first
  `viewport: {375,667}`. Существовал с самого создания регресс-сети
  (2026-07-25), но был невидим: до 2a/2b в приложении не было ни одного
  адаптивного различия, которое проявилось бы по-разному на 375 и 1280px.
  Найдено при добавлении e2e для рельса — `smoke.spec.ts` и три теста
  `pickup-destination.spec.ts` внезапно упали (искали `pickup-pill`,
  которого на десктопе больше нет). Проверено эмпирически
  (`window.innerWidth` внутри теста) перед тем как чинить — реально
  1280×720, не 375×667. Попутно выяснилось, что и мой же новый мобильный
  тест из `desktop-layout.spec.ts` (2a) все это время ложно проходил —
  `expect(locator).not.toBeVisible()` не дожидается стабильности,
  удовлетворяется первым же полом, где элемента ещё нет в DOM (гонка при
  раннем клиентском рендере), не тем, что элемент реально скрыт CSS-ом.
  Исправление — убрать `devices['Desktop Chrome']` из `projects[].use`
  (там нет ничего, что реально нужно: `browserName` и так по умолчанию
  `'chromium'`), оставить глобальный `use.viewport` единственным
  источником дефолтного вьюпорта. Все 14 e2e зелёные после фикса.
- Регресс-сеть: `e2e/desktop-layout.spec.ts` дополнен — рельс не
  оверлей (реальные `boundingBox`, карта начинается ровно там, где
  заканчивается рельс, не перекрытие), полный флоу до `selectingClass`
  в рельсе, дорожный маршрут по-прежнему рендерится (>2 координат — та же
  регрессия, что в `pickup-destination.spec.ts`). Плюс ручной прогон ПОЛНОГО
  флоу заказа (A→Б→класс→поиск→водитель→поездка→оплата→оценка→reset) в
  обоих вьюпортах через ad-hoc-скрипт с реальными таймерами — ноль
  console-ошибок, десктоп визуально чист (карта нигде не перекрыта),
  мобильный не отличим от поведения до 2b.

---

## 2026-07-28 — Десктоп 2c: панель заказа «всё сразу» (compose)

Финальный, самый рискованный кусок десктоп-эпика (`feature/desktop-order-compose`,
план — Opus, реализация — Sonnet). До 2c рельс на десктопе показывал по одной
фазе за раз (2b) — тот же пошаговый флоу, что и мобильный, просто в другом
месте. 2c заменяет это на co-visible панель по референсу Яндекса: адреса
(редактируемые) + грид классов + оплата + «Заказать» видны одновременно, как
только есть маршрут.

- **Авто-переходы — в машине, не в компонентах.** `OrderFlowContext` получил
  `layout: 'mobile' | 'desktop'` (дефолт `'mobile'`), новое событие
  `SET_LAYOUT` (глобальный `on`-хендлер), guard `isDesktopLayout`.
  `OrderScreen` шлёт `SET_LAYOUT` эффектом на `[isDesktop]` (тот же паттерн,
  что уже был у `map.resize()`-эффекта). `selectingPickup` и
  `selectingDestination` получили eventless `always`-переходы, гейтованные
  `and(['hasPickup'/'hasDestination', 'isDesktopLayout'])` — на десктопе
  машина «пролетает» их сама, как только контекст заполняется (геолокация →
  A, выбор Б → сразу в `selectingClass`). На мобилке guard всегда ложный —
  поведение мобилки не тронуто ни на бит (правило проекта №4: XState —
  источник истины для флоу, не `useEffect`-обвязка в компонентах).
  `selectingClass` получил свой `SET_DESTINATION`-хендлер (assign без
  смены состояния) — чтобы правка Б внутри уже открытой панели (драг карты
  / повторный поиск) не «выбрасывала» из compose обратно в
  `selectingDestination`. `resetOrder` теперь сохраняет `layout` при сборке
  нового контекста — иначе «Заказать снова» на десктопе откатывал бы layout
  на дефолтный `'mobile'` и панель ломалась после первого полного цикла.
- **Гибрид выбора адреса (согласовано с Eugene в plan mode).** И текстовые
  поля «Откуда»/«Куда» (клик → инлайн-поиск, как у Яндекса), И
  перетаскивание карты для точки Б (как было в 2b) — оба механизма шлют
  `SET_DESTINATION`/`SET_PICKUP`. Явно НЕ добавляли «Промокод»/«Комментарий
  водителю» — вне скоупа 2c, можно вернуть отдельным мелким пунктом позже.
- **`useDestinationSync` (новый, `features/order-flow/`)** — вынесенная из
  `DestinationSheet` data-логика синка адреса Б: поиск, reverse-geocode
  центра карты при user-drag, OSRM-prefetch, `candidateDestination`. Общая
  для `DestinationSheet` (мобилка) и `OrderComposePanel` (десктоп).
  **Визуальная физика шторки (`snap`/drag/retreat) НЕ вынесена** — она
  осталась только в `DestinationSheet`, десктопу ретритить нечего (статичный
  блок). Хук принимает колбэки `onUserMoveStart`/`onUserSettle`/`onPick`:
  мобилка передаёт только первые два (retreat-анимация, `SET_DESTINATION`
  шлётся по кнопке-подтверждению, как раньше); десктоп передаёт
  `onUserSettle`+`onPick`, оба сразу шлют `SET_DESTINATION` — в compose-
  панели нет отдельного шага подтверждения. Колбэки читаются через
  latest-ref-паттерн (не в deps эффекта) — иначе inline-стрелки на местах
  вызова пересоздавали бы map-листенеры на каждый рендер.
- **`ClassGrid` (новый) — вынесенный презентационный грид** карточек класса
  из `ClassPickerSheet`, используется и там (внутри `OrderSurface`), и в
  `OrderComposePanel` — один источник разметки для обоих брейкпоинтов.
- **`PaymentRow` (новый)** — строка способа оплаты в панели, читает
  `useGetPaymentMethodsQuery` (сущность уже была). Косметика: выбор метода
  живёт в локальном стейте компонента, реальную оплату машина по-прежнему
  фиксирует на `completed` через `RideCompletionSheet` — тут просто
  предпросмотр/переключатель дефолтного способа, как у Яндекса.
- **`PickupSheet`/`DestinationSheet` — desktop-ветки убраны, не «на всякий
  случай» оставлены.** Их `isDesktop`-рендер использовал `OrderScreen`
  только через `!isDesktop &&`-ветку монтирования (2b) — с 2c десктоп идёт
  через `OrderComposePanel`, эти компоненты стали чисто мобильными,
  `useIsDesktop()` из них выпилен. Мёртвый код не оставляли «про запас».
- **Реальный баг, найденный при проверке гибрида глазами (не тестом):
  порядок эффектов монтирования vs `mapRef.current`.** `OrderComposePanel`
  живёт в `<aside>` (рельс), который в JSX/DOM стоял РАНЬШЕ `<MapCanvas>`.
  React обходит дерево и запускает mount-эффекты в порядке рендера
  (bottom-up по поддереву, но между соседними поддеревьями — строго по
  DOM-порядку: первое поддерево долистывает все свои эффекты раньше, чем
  начинается следующее). `MapCanvas` синхронно пишет `mapRef.current = map`
  в СВОЁМ mount-эффекте, сразу после конструктора `new maplibregl.Map(...)`
  (не дожидаясь события `load`, см. `MapCanvas.tsx`) — но раз рельс шёл
  первым в дереве, эффект `useDestinationSync` внутри `OrderComposePanel`
  успевал выполниться (и молча выйти по `if (!map) return`) ДО того, как
  `MapCanvas` вообще успевал создать карту. Слушатели `dragstart`/`moveend`
  никогда не навешивались — драг карты для точки Б не работал бы на
  десктопе вообще, при том что build/lint/e2e (клик-based сценарии, не
  drag) были зелёными. Нашёл руками — потыкал drag на скриншотах
  ad-hoc-скрипта, увидел, что Б не двигается. **Фикс**: физически переставил
  `<div data-slot="order-map-area">` (с `MapCanvas`) ПЕРЕД `<aside>` в
  JSX/DOM, визуальный порядок (рельс слева) вернул через Tailwind
  `order-first` на `<aside>` — тот же паттерн, что и «в DOM после карты, на
  экране раньше», просто явно комментированный, чтобы не откатили при
  следующей правке. `DriverCard`/`DriverSearchPanel`/`RideCompletionSheet`/
  `RideDoneCard` этот баг не задевал — они не используют `mapRef` на mount.
- **Второй баг того же типа: дропдаун результатов поиска Б не закрывался
  после выбора.** Список результатов под полем «Куда» рендерится по
  `isDestinationFocused`, а строки — обычные `<button>` внутри поля с
  `onMouseDown={preventDefault}` на обёртке (нужно, чтобы `blur` инпута не
  снял список ДО того, как успеет сработать `onClick` по строке — иначе
  клик по результату не долетал бы вовсе). Побочный эффект: раз `blur`
  подавлен намеренно, `isDestinationFocused` никогда не сбрасывался сам —
  список оставался открытым и перекрывал грид классов/оплату/«Заказать»
  даже после успешного выбора. Нашёл тем же ad-hoc-прогоном (скриншот после
  клика показал: цена посчиталась, но панель под списком не видна).
  Исправлено — явный `setIsDestinationFocused(false)` в обёртке-хендлере
  выбора строки (`handlePickDestinationRow`), а не полагаться на blur.
- **`e2e/desktop-layout.spec.ts` переписан под compose**: вместо шагов
  «pickup-where-to → destination-rail → confirm → Выберите класс» — один
  тест печатает адрес в «Куда», кликает результат в
  `[data-slot="compose-to-results"]` и проверяет co-visible
  `compose-order`/`compose-payment`, дорожный маршрут (>2 координат) и что
  «Заказать» доводит до `searchingDriver`. Тест на скрытие мобильного
  `ProfileButton` тоже переписан — раньше проверял «смонтирован, но
  `lg:hidden`» (ровно 2 узла), но с 2c `always`-переход выходит из
  `selectingPickup` (что гейтует монтирование `ProfileButton`) почти сразу
  после резолва геолокации, так что кнопка на десктопе часто вообще не
  смонтирована — переписан на инвариант «ровно один ВИДИМЫЙ элемент»
  (`:visible`-псевдокласс), не завязанный на факт монтирования.
- Проверено: `npm run build`/`lint`/`test:e2e` (все 14, включая полный
  мобильный `full-ride.spec.ts`) зелёные. Глазами в обоих вьюпортах через
  ad-hoc-скрипт (реальные MapTiler/OSRM, не e2e-моки) — десктоп: адрес A
  резолвится, поиск Б, выбор из списка, грид классов + оплата + «Заказать»
  co-visible, драг карты на живую пересчитывает Б и цену (подтверждено
  скриншотами до/после драга), полный переход в `searchingDriver`; мобилка
  — пошаговый флоу не отличим от поведения до 2c. Ноль console-ошибок в
  обоих прогонах.

Десктоп-адаптив (2a/2b/2c) полностью завершён.

---

## 2026-07-28 — Багфикс: линия маршрута «стирается» позади такси (А→Б)

Первый пункт из ревью Eugene по живому демо (`fix/route-line-erase`, план —
Opus, реализация — Sonnet). Во время `inRide` (такси везёт пассажира А→Б)
линия маршрута висела статично от А до Б всю поездку — маркер просто ехал
поверх. Ожидаемо (как у Яндекса): линия должна «стираться» позади машины —
маркер как ластик.

**Согласовано с Eugene:** чиним для обоих вьюпортов сразу (код общий,
не завязан на брейкпоинт); стирание только для отрезка А→Б (`inRide`) —
этап «водитель едет к А» (`enRoute`) сейчас линии не рисует вообще, не
трогаем, это отдельная более крупная фича.

- **Один `locate()` — гарантия отсутствия шва.** Маркер и обрезанный край
  линии должны совпадать день в день, иначе на стыке виден зазор/нахлёст.
  Вынес геометрию сегмент-обхода полилинии (раньше жила только внутри
  `useAnimatedPosition.ts` как приватная `positionAlongPolyline`) в новый
  `shared/geo/polyline.ts` — общий приватный `locate(polyline, progress)` →
  `{ point, index }`, на нём построены обе публичные функции:
  `positionAlongPolyline` (для маркера, идентична прежнему поведению) и
  новая `remainingPolyline` (для линии) — `[point, ...polyline.slice(index+1)]`.
  Оба вызова с одним и тем же `progress` дают согласованные точки — голова
  линии буквально совпадает с позицией маркера, не приблизительно.
- **`progress` протащен наружу из `useAnimatedPosition`** — было
  `LatLng | null`, стало `{ position, progress }`. `progress` — тот же `t`,
  что уже считался в rAF-тике, просто не выбрасывался. Единственный
  потребитель (`useRideAutomation`) обновлён.
- **`useRideAutomation` отдаёт `remainingRouteLine`** — во время `inRide`
  считает `remainingPolyline(inRidePolyline, progress)`, иначе `null`.
  `inRidePolyline` — та же геометрия А→Б, что рисует `OrderScreen` (общий
  `useGetRouteQuery` кэш, см. существующий комментарий про дедуп) — резать
  можно прямо тут, откуда рядом и полилиния, и прогресс.
- **`OrderScreen`**: `routeLine` = обрезанная линия на `inRide` (если
  валидна, ≥2 точек), иначе полная А→Б — как раньше. Все остальные фазы
  (включая `enRoute`) не тронуты. Каждый кадр `inRide` даёт свежий массив
  → `MapCanvas.setData` каждый кадр — ожидаемо и не мемоизируется отдельно
  (сам маркер уже ре-рендерит `OrderScreen` покадрово).
- **Регресс — `e2e/full-ride.spec.ts`**: inRide-нога, раньше проматываемая
  одним `fastForward(6200)`, разбита на `fastForward(3000)` (середина,
  ещё в полёте) + повторное чтение `route-line`-координат + финальные
  `fastForward(3200)`. Ассерты: координат стало меньше, чем в полной линии,
  и первая координата сместилась от А. **Гонка, найденная при первом
  прогоне**: `page.clock.fastForward()` может резолвиться на бит раньше,
  чем React закоммитит последний rAF-triggered апдейт state → DOM/карту —
  единичное чтение сразу после `fastForward` иногда ловило старое
  состояние (5 координат вместо ожидаемых меньше). Тот же класс гонки, что
  уже решён для driver-search retry в этом файле — тот же фикс: обернул
  чтение в `expect(async () => {...}).toPass({timeout: 2000})` вместо
  одиночного чтения.
- **Проверка глазами (реальные MapTiler/OSRM, не e2e-моки) выявила
  два ложных срабатывания в моём же ad-hoc-скрипте, не в продукте** —
  стоит записать, чтобы не наступить снова:
  - Скриншот сразу после клика «Начать поездку» без паузы ловил
    транзиентный кадр, где `driverPosition` ещё `null` (эффект
    `useAnimatedPosition` для новой ноги ещё не отдал первый rAF-тик) —
    не баг, артефакт момента снятия скриншота.
  - На мобильном прогоне маршрут получился вырожденным (Б === А) —
    потому что тестовый скрипт кликал «Куда едем?» и сразу
    «Подтвердить», ни разу не подвинув карту и не выбрав другое место
    (в отличие от десктопного прогона, где реально искал «Красная
    площадь»). Карта не двигалась → `map.getCenter()` на подтверждении
    возвращал те же координаты, что и pickup. Побайтово идентичные
    скриншоты подряд — это и выдало проблему; после починки скрипта
    (клик по реальному недавнему адресу, как в существующих
    `pickup-destination.spec.ts`) стирание корректно подтвердилось и на
    мобилке. Оставлено здесь как напоминание: вырожденный А===Б маршрут
    даёт именно такую картину (частота обновлений сохраняется, но
    геометрия не двигается) — не путать с реальным зависанием анимации.
- Проверено: `npm run build`/`lint`/`test:e2e` (14/14) зелёные. Глазами в
  обоих вьюпортах (реальные MapTiler/OSRM) — серия скриншотов по ходу
  `inRide`: линия стабильно стартует от маркера, не тянется назад к уже
  проеханному, корректно укорачивается к моменту завершения поездки. Ноль
  console-ошибок в обоих прогонах.

---

## 2026-07-28 — Багфикс: маршрут рисуется прямой линией на далёких адресах

Второй пункт ревью Eugene (`fix/route-line-straight-on-far-jump`) — на
экране выбора класса линия маршрута для далёких адресов (пример со скрина —
Ростов→Аксай) оставалась прямой, хотя OSRM честно возвращал геометрию по
дорогам. Не связано с прошлым багфиксом (стирание при `inRide`) — отдельный,
ранее не замеченный дефект в `shared/map/MapCanvas.tsx`.

**Причина, подтверждённая экспериментально** (dev + реальный MapTiler/OSRM,
дебаг-лог полного пути `pickup/destination/routeData` в `OrderScreen` +
прямое чтение `map.getSource('route-line')._data` в браузере — не
предположение): эффект применения линии был устроен так —
```ts
const run = () => applyRouteLine(map, routeLine)
if (map.isStyleLoaded()) run()
else map.once('load', run)
```
`map.isStyleLoaded()` временно возвращает `false` не только при первой
загрузке карты, но и пока MapLibre догружает тайлы для новой области —
ровно то, что происходит сразу после большого `jumpTo`/`fitBounds` при
выборе далёкого адреса. Эффект уходил в `else` и подписывался на
`map.once('load', run)` — но `'load'` у объекта карты срабатывает **один
раз за всю жизнь**, в самом начале, уже отработав к этому моменту.
Повторная подписка никогда не выстреливала — реальная геометрия (в репро —
383 точки, `useGetRouteQuery` честно вернул `status: fulfilled`) терялась
безвозвратно, на карте оставалась последняя успешно применённая линия —
прямая, нарисованная как `roadOrStraight`-fallback, пока запрос ещё летел.
Стабильно ловится именно на далёких адресах — больше прыжок камеры → дольше
грузятся тайлы → шире окно, в которое попадает момент прихода реального
маршрута; на близких адресах ответ обычно успевает прийти, пока
`isStyleLoaded()` ещё `true`, потому и не было замечено раньше.

Тот же класс проблемы («`isStyleLoaded()` не заслуживает доверия
синхронно») уже чинили для смены темы (`setStyle`, 2026-07-25, dark-map) —
но решение здесь **проще**, не копия того фикса: `applyRouteLine` уже умеет
безопасно звать `source.setData()` в любой момент, даже пока карта
догружает тайлы (этим же путём работает покадровое обновление линии во
время стирания в `inRide`, см. предыдущую запись) — `isStyleLoaded()`
реально нужен только для **первого создания** source+layer, не для каждого
апдейта уже существующего source. Новая версия эффекта:
```ts
if (map.getSource(ROUTE_LINE_SOURCE_ID) || map.isStyleLoaded()) {
  applyRouteLine(map, routeLine)
  return
}
const apply = () => applyRouteLine(map, routeLineRef.current)
map.once('idle', apply)
return () => { map.off('idle', apply) }
```
`'idle'` (в отличие от `'load'`) срабатывает каждый раз, когда карта
успокаивается после догрузки — не одноразовое событие, так что ветка-
страховка для «source ещё нет И стиль ещё не загружен» тоже надёжна.
`ROUTE_LINE_SOURCE_ID`/`routeLineRef` — уже существовавшие в файле
переменные, не новые.

**Осознанно без нового e2e-теста** (тот же прецедент, что у dark-map): под
`EMPTY_STYLE` (мок стиля для e2e, `sources:{}, layers:[]`) `isStyleLoaded()`
всегда `true` — условие бага физически не воспроизводимо под моками, тест
не смог бы покраснеть на этом дефекте. Существующие e2e-ассерты «road-
following route, coords > 2» остаются зелёными (идут по немедленной ветке
как и раньше). Проверено вместо этого ad-hoc-скриптом на реальных тайлах —
повторил сценарий Eugene (геолокация Ростов, поиск «Аксай»): десктоп — 383
точки геометрии (совпадает с числом из репро), мобилка — 8711 (более
детальный маршрут OSRM для выбранного результата), плюс близкий адрес
(«Ворошиловский проспект», 60 точек) — не сломали обычный случай. Все линии
на скриншотах идут по реальным дорогам с поворотами, не прямой диагональю.
`npm run build`/`lint`/`test:e2e` (14/14) зелёные, ноль console-ошибок в
обоих ad-hoc-прогонах.

## 2026-07-28 — Багфикс: реальная геолокация терялась (карта показывала Москву)

Третий пункт ревью Eugene — репорт был про Профиль (открыть/закрыть →
карта на Москве вместо реального GPS в Ростове), но причина оказалась
**шире репорта**: воспроизводилась уже на самой первой загрузке, без
всякого захода в Профиль. Найдено и подтверждено ad-hoc (`playwright`,
реальная геолокация Ростов, временные debug-логи в `PickupResolver`/
`PickupSheet`, удалены после проверки) — не по чтению кода.

**Главная причина.** `PickupSheet.tsx` вешал `moveend`-слушатель на карту и
трактовал ЛЮБОЕ «успокоение» карты как пользовательский drag точки А,
сразу коммитя `map.getCenter()` через `SET_PICKUP`. `moveend` стреляет и на
программные движения — включая самое первое успокоение карты сразу после
конструирования (`MapCanvas` создаётся с хардкод-центром `DEMO_PICKUP` =
Москва). Это первое `moveend` срабатывает через ~15–25мс после монтирования
(лог: дважды подряд ДО того, как `useCurrentPosition` вообще успевает
получить `success`) — практически всегда побеждая гонку с реальной
(асинхронной) геолокацией. `pickup` фиксировался на Москве; когда реальная
геолокация всё же резолвилась, `PickupResolver` уже видел `pickup !== null`
и по своему гварду молча пропускал — координаты терялись безвозвратно, не
только после Профиля.

**Вторичная причина (для сценария Профиля конкретно).** `MapCanvas` читает
`center`-проп только в момент `new maplibregl.Map(...)`. При уходе в
Профиль `OrderScreen`/`MapCanvas` полностью размонтируются (плоский
переключатель экранов в `App.tsx`) и создаются заново с хардкодом
`center={DEMO_PICKUP}` при возврате — `pickup` в XState-акторе персистит
(актор живёт над переключателем), так что `PickupResolver` снова пропускает
по гварду, и пересозданную карту нечему подвести назад к реальному pickup.
Не гонка — детерминированный сброс на каждый round-trip.

**Почему фикс — `dragstart`, а не `moveend.originalEvent`** (эмпирика,
три ad-hoc прогона — не допущение, MapLibre-события в проекте уже дважды
кусали, `once('load')`/`isStyleLoaded()` выше): `moveend.originalEvent` есть
у реального drag (`mouseup`), но у **wheel-zoom его нет вообще** — ни на
`moveend`, ни на `movestart` (зум идёт через внутреннюю rAF-анимацию,
исходное DOM-событие не пробрасывается). Гейт по `originalEvent` тихо
проглотил бы зум-обновления, которые старый код поддерживал — это была бы
регрессия. `dragstart` — чистый сигнал: стреляет исключительно на реальный
пользовательский pan-жест, никогда на `jumpTo`/начальное успокоение/зум
(подтверждено прямыми пробами на реальной карте).

**Правки:**
- `PickupSheet.tsx` — `moveend`→`SET_PICKUP` теперь гейтится флагом,
  поднимаемым на `dragstart` и сбрасываемым на каждом `moveend`. Заодно
  убран `isLeavingRef` (костыль против `handlePickRecent`'s `jumpTo`) — он
  больше не нужен, `jumpTo` и так не поднимает `dragstart`.
- `OrderScreen.tsx:130` — `MapCanvas`'s `center` строится по реальному
  `pickup` из контекста, если он уже есть, иначе фолбэк на `DEMO_PICKUP`.
  Чинит не только Профиль, а любой уход/возврат на соседний экран (история,
  оплата, адреса, настройки — все ремонтируют `MapCanvas`).

**Осознанная смена поведения:** точка А теперь обновляется только на
реальный drag, не на зум (ближе к модели Яндекса — «тащи карту, чтобы
двигать пин», зум — это просто рассмотреть). Раньше зум тоже двигал А
(случайно, тем же багнутым `moveend`-путём). Явно отмечено Eugene.

**Регресс-тест добавлен** (`e2e/geolocation-persistence.spec.ts`) — в
отличие от far-jump выше, причина здесь **style-agnostic** (события +
XState + center-проп, не MapLibre tile-timing), поэтому воспроизводима под
`EMPTY_STYLE` е2e-моком. Ключ — `test.use({ geolocation: Ростов })`,
переопределяющий глобальный дефолт (`playwright.config.ts`), который
случайно совпадал с `DEMO_PICKUP` (Москва) и потому маскировал баг во всей
остальной регресс-сети. Три проверки: центр карты не на Москве сразу после
загрузки (ловит гонку), не сбрасывается на Москву после Профиль→назад
(ловит детерминированный сброс), drag всё ещё двигает точку А (не сломали
основную интеракцию гейтом).

**Побочная находка при первом прогоне (не баг продукта):** e2e неожиданно
показал реальные MapTiler-адреса вместо мокнутых — причина оказалась в
собственном окружении, не в коде: фоновый `npm run dev`, оставленный
запущенным с прошлой ad-hoc-сессии (plan-mode проверка `dragstart`),
`reuseExistingServer` в `playwright.config.ts` подхватил его вместо
собственного (у чужого процесса не было `VITE_E2E=true` → MSW не
регистрировал `e2eHandlers` → reverse-geocode реально бил в MapTiler).
Подтверждено через `git stash` на чистом коде — тот же симптом,
воспроизводится независимо от этой правки. Убедиться, что порт 5173
свободен, прежде чем гонять `test:e2e` после ad-hoc-сессий с ручным `npm
run dev`.

**Проверка:** `npm run build`/`lint`/`test:e2e` (17/17, включая 3 новых)
зелёные. Ad-hoc глазами (dev, реальный MapTiler/OSRM, геолокация Ростов,
оба вьюпорта): карта на Ростове при загрузке и остаётся там после
Профиль→назад в обоих случаях, ноль console-ошибок.

## 2026-07-28 — Таб навбара «Пассажирам» становится реальной навигацией

Первая из трёх фич с одинаковой моделью (Пассажирам → Водителям → Бизнесу,
по одной, Eugene присылает референс-скриншоты по разделам отдельно, не всё
сразу). Табы `DesktopNavbar` (`Пассажирам`/`Водителям`/`Бизнесу`/`Помощь`)
были текстовыми плейсхолдерами без действия с самого 2a (2026-07-27) —
теперь первый из них ведёт на настоящую страницу.

**Решения, подтверждённые Eugene заранее (см. предыдущее сообщение в
диалоге, вопросы через AskUserQuestion):**
- **Один уровень навигации.** Карточки внутри страницы — визуальная
  группировка тем (иконка + заголовок + короткое описание), НЕ клик дальше
  на под-страницы. Референс Яндекса (go.yandex/ru_ru/lp/safety) двухуровневый
  (грид карточек → клик → отдельная страница по теме) — сознательно
  упростили: изначальный запрос Eugene был «кидают куда-нибудь, где просто
  текст лежит с описанием», один уровень ближе к этому и быстрее в
  реализации.
- **Вход с обоих вьюпортов**, не только с десктопа, где физически живёт
  навбар (`DesktopNavbar` — `hidden lg:flex`). На мобильном — новый пункт
  в существующем гриде быстрых ссылок на экране Профиля (`ListRow`,
  тот же паттерн, что «История»/«Настройки» и т.д.) — не изобретали
  отдельный UI-элемент для входа.

**Контент — не клон.** Референс-скриншоты Яндекса показывают страницу
общей «безопасности» (пассажиры И водители на одной странице), с темами
вроде COVID-19/самокатов — нерелевантными для этого демо. Взяли только
СТРУКТУРУ (интро-параграф + грид карточек под заголовком), контент свой,
под контекст такси-приложения: Безопасность в поездке, Страхование
поездок, Конфиденциальность данных, Финансовая безопасность, Безопасность
детей, Поддержка 24/7. Иконки — `lucide-react` (уже используется по всему
приложению, не кастомные 3D-иллюстрации Яндекса).

**Экран `PassengersScreen.tsx` — на существующем `ScreenShell`**, не
отдельный full-bleed маркетинговый layout. Консистентность с остальными
контентными экранами (`InfoScreen`/`SupportScreen`/`RideHistoryScreen`) —
та же capped-колонка (`lg:max-w-2xl`), тот же заголовок+«Назад». Если
Eugene захочет шире/наряднее под маркетинг-страницу — отдельный лёгкий
follow-up, не блокирует эту фичу.

**Back-таргет зависит от вьюпорта.** Навигация в приложении плоская
(`useState<Screen>`, без history-стека, см. `NavigationProvider.tsx`) — нет
«откуда пришли», это нужно решать явно на каждом экране. Десктоп попадает
на страницу с навбара (peer экрана заказа) → назад на `order`. Мобильный —
из Профиля → назад в `profile`. `useIsDesktop()` уже существовал (эпик
десктоп-адаптива) — переиспользован, не новый механизм.

**`MENU_ITEMS` в `DesktopNavbar.tsx`** сменил тип с `string[]` на
`{ label: string; screen?: Screen }[]` — только «Пассажирам» получил
`screen: 'passengers'`, три остальных пункта пока без `screen` (рендерятся
прежним неактивным `<span>`) до своих фич. Активный таб подсвечивается
(`screen === item.screen`), уже есть `screen` из `useNavigation()`.

**Контент — статический инлайн-массив**, не RTK Query/MSW: это не
серверные (мокнутые) данные, а фиксированный текст приложения — правило
проекта «RTK Query только для серверных данных» здесь не применимо, тот
же подход, что `FAQ_ITEMS` в `SupportScreen`/инлайн-текст в `InfoScreen`.

**Осознанно не выделяли общий компонент** для инфо-страниц (интро+грид) —
пока есть только один экземпляр. Извлечём при второй фиче («Водителям»),
по правилу трёх — преждевременная абстракция на одном примере рискует
угадать не тот API.

**E2E:** новый `e2e/passengers-page.spec.ts` — десктоп (таб навбара →
страница → «Назад» на рельс заказа) и мобильный (Профиль → страница →
«Назад» в Профиль), плюс проверка на console-ошибки. По пути поймана
и решена коллизия: `page.getByText('Пассажирам', {exact:true})` на
мобильном вьюпорте матчил ДВА элемента — видимую кнопку в Профиле и
скрытый (`display:none` через `hidden lg:flex`) таб `DesktopNavbar`,
который физически остаётся в DOM. `getByText` не фильтрует по видимости;
`getByRole('button', {name:...})` — фильтрует (скрытые через `display:none`
элементы исключаются из accessibility-дерева), поэтому однозначно матчит
только видимую мобильную кнопку. Переключились на `getByRole` везде в
этом тесте.

**Проверка:** `npm run build`/`lint`/`test:e2e` (19/19, включая 2 новых)
зелёные. Ad-hoc глазами оба вьюпорта (скриншоты, dev-сервер) — десктоп:
грид 2 колонки, активный таб подсвечен зелёным текстом; мобильный: грид 1
колонка, карточки читаемы. Ноль console-ошибок в обоих прогонах.

## 2026-07-29 — Таб «Водителям» + извлечение общего `TopicPageScreen`

Вторая из трёх фич навбар-табов (Пассажирам → **Водителям** → Бизнесу).
Референс — скриншоты Eugene с `taxi.yandex.ru/rabota` (Яндекс.Про): тёмный
рекрутинговый лендинг — hero-секция (заголовок «Работа в такси с
партнёрами», плашка «Доход водителя до 242 000 ₽», чипы «Выплаты каждый
день / Гибкий график / Поддержка 24/7», жёлтая CTA «Оставить заявку»), ниже
— промо-блоки («Яндекс Гараж» аренда авто, «Будьте водителем в свободное
время» с 3 карточками-бенефитами).

**Структурно референс сильно другой, чем «Пассажирам»** (маркетинговый
hero-лендинг vs. спокойная инфо-страница) — задал явный развилочный вопрос
через AskUserQuestion до плана. Eugene выбрал:
- **Стиль — как «Пассажирам»**, не тёмный hero-лендинг. Берём только смысл
  (что предлагаем водителю), не драматургию референса.
- **CTA «Оставить заявку» всё-таки нужен** (отдельный вопрос про поведение
  кнопки) — клик → мок «Заявка отправлена» (`useState`, тот же паттерн, что
  «Написать в поддержку» на `SupportScreen`), реальной формы/бэкенда нет.

Итог — совмещение обоих ответов: та же спокойная инфо-структура, что
Пассажирам, плюс одна CTA-кнопка под гридом (которой у Пассажирам нет).

**Контент — не клон.** 6 карточек под наше такси: Выплаты каждый день,
Гибкий график, Поддержка 24/7, Аренда авто, Система бонусов, Доход под
ваши цели. Без «Яндекс Гараж»/конкретного города/фото водителей из
референса — только структура и общий смысл предложения.

**Извлечён `src/screens/_shared/TopicPageScreen.tsx` — по правилу трёх.**
При «Пассажирам» (2026-07-28) осознанно НЕ выделяли общий компонент — один
инстанс недостаточен, чтобы угадать правильный API. Теперь Пассажирам и
Водителям — одинаковая структура (интро + грид карточек), Бизнесу впереди
третьим — самое время. Шаблон инкапсулирует:
- `ScreenShell` + `title`.
- **Back-таргет по вьюпорту** (`useIsDesktop()` — десктоп → `order`,
  мобильный → `profile`) — было продублировано в `PassengersScreen`,
  теперь в одном месте.
- Интро-параграф + грид карточек (иконка/заголовок/описание) — тот же
  markup/классы, что были в `PassengersScreen`.
- `children`-слот под гридом — опциональный (Пассажирам не использует,
  Водителям передаёт CTA-кнопку).

Живёт в `screens/_shared/`, не `shared/ui/` — компонент навигационно-
осведомлён (`useNavigation`/`useIsDesktop`), тот же принцип, что уже
разделяет `DesktopNavbar` (`screens/_layout/`, app-aware) от `ScreenShell`
(`shared/ui/`, принимает `onBack` пропом, ничего не знает о `Screen`).

**`PassengersScreen` отрефакторен на шаблон** — сведён к данным (`TOPICS` +
`INTRO`) через `<TopicPageScreen>`. Рендер обязан был остаться байт-в-байт
тем же (те же классы/markup) — подтверждено: существующий
`e2e/passengers-page.spec.ts` прошёл **без единой правки**, что и есть
доказательство безопасности рефакторинга (тест бы покраснел на любом
визуальном/структурном расхождении, которое он проверяет).

**E2E:** новый `e2e/drivers-page.spec.ts` — десктоп (таб → страница → клик
CTA → кнопка задизейблена с текстом «Заявка отправлена» → «Назад» на рельс
заказа) и мобильный (Профиль → страница → «Назад» в Профиль), плюс
console-ошибки. Сразу использует `getByRole('button', {name:...})` (не
`getByText`) — грабля с коллизией скрытого навбар-таба на мобильном
вьюпорте была найдена и решена в прошлой фиче (Пассажирам), тут просто
применена с самого начала.

**Проверка:** `npm run build`/`lint`/`test:e2e` (21/21, включая 2 новых)
зелёные — весь набор, включая нетронутый `passengers-page.spec.ts`. Ad-hoc
глазами оба вьюпорта (скриншоты, dev-сервер, порт 5173 предварительно
освобождён — грабля прошлой фичи): десктоп — активный таб подсвечен, грид 2
колонки, CTA переключается в приглушённое «Заявка отправлена»; мобильный —
грид 1 колонка. Отдельно перепроверена страница «Пассажирам» после
рефакторинга — визуально идентична прежним скриншотам. Ноль console-ошибок
во всех прогонах.

## 2026-07-29 — Таб «Бизнесу» — третья и последняя фича линии навбар-табов

Референс — скриншоты Eugene с `business.go.yandex` (Яндекс Go для бизнеса):
жёлтая маркетинговая хиро-плашка «Такси для бизнеса» с описанием и двумя
кнопками («Подключить компанию» / «Получить консультацию») рядом с фото,
затем «Преимущества такси для бизнеса» — 4 карточки на светло-сером фоне
(Экономия, Контроль, Удобная отчётность, Доступность), затем «Рабочие
поездки проще с нашими опциями» — 2 широкие карточки (Развоз сотрудников,
«Час+»).

**Решение по стилю уже принято на «Водителям»** (2026-07-29, предыдущая
запись) — не повторяем маркетинговый хиро, тот же спокойный
`TopicPageScreen`. Никакого нового развилочного вопроса не понадобилось.

**Контент — объединение и адаптация**, не клон:
- Оба грида референса («Преимущества» + «Опции») слиты в один грид из 6
  карточек: Экономия, Контроль, Удобная отчётность, Развоз сотрудников,
  Почасовая аренда, Доступность.
- «Работаем более чем в 700 городах России» → «Работаем в городах по всей
  стране» — конкретное число для фиктивного демо-сервиса не выдумывали
  (тот же принцип, что и раньше — не изображать несуществующие факты).
- Две CTA-кнопки референса → одна («Подключить компанию»), консистентно с
  «Водителям» (там тоже одна CTA, хотя источник вдохновения — просто
  паттерн `SupportScreen`, не референс).

**Реализация тривиальна** благодаря `TopicPageScreen`, извлечённому на
прошлой фиче — по плану предсказали это явно. Только `BusinessScreen.tsx`
(данные, `useState` для CTA — копия структуры `DriversScreen.tsx`) + три
точки навигации (`Screen` union, `App.tsx` роут, `screen: 'business'` в
`DesktopNavbar.MENU_ITEMS`, `ListRow` в `ProfileScreen`). Все три
навбар-таба из линии (Пассажирам/Водителям/Бизнесу) теперь ведут на
реальные страницы — из `MENU_ITEMS` остался только «Помощь» без `screen`.

**E2E:** `e2e/business-page.spec.ts` по образцу `drivers-page.spec.ts` —
`getByRole` с самого начала (не `getByText`), десктоп (таб → страница →
CTA → «Заявка отправлена» disabled → назад на рельс) и мобильный (Профиль
→ страница → назад). Существующие `passengers-page`/`drivers-page` спеки
остались зелёными без единой правки — шаблон не трогали, только новые
данные, тот же аргумент безопасности, что при извлечении шаблона.

**Явно отложено на следующую фичу** (Eugene сказал в этом же сообщении,
где прислал референс «Бизнесу»): «прикольнее» стиль карточек — по
референсам Яндекса плашки более выразительные (мягкие скруглённые формы
без явной рамки, контрастные фоны секций), но **не жёлтый акцент
Яндекса** — свой изумрудный (`--primary`, зафиксирован в
`.claude/rules/design-tokens.md`). Эта правка коснётся `TopicPageScreen` и
поэтому обновит визуал всех трёх страниц разом — содержательная фича, не
мелкая правка, специально не сделана мимоходом в этой.

**Проверка:** `npm run build`/`lint`/`test:e2e` (23/23, включая 2 новых)
зелёные. Ad-hoc глазами оба вьюпорта (скриншоты, dev-сервер, порт 5173
предварительно освобождён) — десктоп: активный таб «Бизнесу» подсвечен,
грид 2 колонки, CTA переключается в приглушённое «Заявка отправлена»;
мобильный: грид 1 колонка. Ноль console-ошибок в обоих прогонах.

## 2026-07-29 — «Дорогой» редизайн карточек `TopicPageScreen`

Eugene хотел повторить визуал референсов Яндекса — «красивые прямоугольные
плашки» — на трёх уже готовых навбар-таб-страницах. Уточнил заранее (в
диалоге, до plan mode): направление — крупнее скругления, мягкая подложка
вместо рамки, hover-эффект. На прямой вопрос про клик-аффорданс («карточки
у Яндекса кликабельны, у нас мок везде») ответил explicitly: **«чисто
ховер эффект мне нравится»** — то есть карточки не должны выглядеть
кликабельными (некуда вести, у нас один уровень навигации, решено ещё на
«Пассажирам»).

**Правка ровно в одном месте — `TopicPageScreen.tsx`** — обновила все три
страницы (Пассажирам/Водителям/Бизнесу) разом. Это прямое подтверждение
пользы извлечения шаблона по правилу трёх (см. запись 2026-07-29,
«Водителям») — редизайн-фича, которая иначе потребовала бы синхронной
правки трёх копипаст-файлов, свелась к одному.

**Карточка, было → стало:**
- `border border-border` → `bg-card shadow-sm` — убрали жёсткую рамку в
  пользу мягкой подложки/тени.
- `rounded-xl` → `rounded-2xl` — крупнее скругление, ближе к «плашкам»
  референса.
- `p-4` → `p-4 sm:p-6` — компактнее на мобилке (Eugene просил «меньше»),
  просторнее на десктопе.
- Добавлен `transition-shadow duration-200 hover:shadow-md` — тень
  чуть углубляется на hover. Карточка осталась `<div>` (НЕ `<button>`),
  без `cursor-pointer`, без press/translate-состояния — намеренно, чтобы
  не читалось как «нажми меня» (прямое следствие ответа Eugene выше). На
  мобилке hover физически не существует — там просто статичные плашки.
- Иконка-чип: `bg-muted`/`text-foreground` → `bg-primary/10 text-primary`
  — изумрудный акцент (легитимный продуктовый цвет, не `--accent`, см.
  `.claude/rules/design-tokens.md`) вместо нейтрального серого. Один
  акцентный штрих — то, что отличает «дорого» от «просто покруглее».
- Интро-параграф `text-sm` → `text-base` — небольшая полировка вводного
  текста.

**Подъём карточки работает в обеих темах, но по РАЗНЫМ механизмам** —
заземлено по токенам `src/index.css` до реализации (не предположение):
светлая тема — `--card` (oklch 1 0 0) равен `--background` (oklch 1 0 0),
т.е. буквально белый на белом → подъём даёт **только тень**. Тёмная тема —
`--card` (0.205) заметно светлее `--background` (0.145) → подъём даёт
**тональную разницу фона** (в тёмной тени такой глубины, как в светлой,
почти не видны на тёмном фоне). Поэтому карточка — обязательно `bg-card` +
тень вместе, не одно из двух: тень одна не сработала бы в тёмной, `bg-card`
один не дал бы читаемого подъёма в светлой (там `--card`==`--background`).

**Что осознанно не делали** (чтобы фича осталась сфокусированной, решено в
плане заранее): не расширяли контейнер страницы (`ScreenShell` остался
`lg:max-w-2xl`, грид `sm:grid-cols-2`) — референс Яндекса широкий, 3-4
карточки в ряд, но `ScreenShell` общий для многих экранов приложения,
расширение — отдельная ось риска вне скоупа этой правки. Не добавляли
крупные центрированные секции-заголовки/жёлтый хиро — маркетинг-layout,
уже отклонённый Eugene на «Водителям».

**Без нового e2e** — чисто визуальное изменение, тест не может осмысленно
«покраснеть» на «выглядит дорого». Существующие `passengers-page`/
`drivers-page`/`business-page` спеки прошли зелёными **без единой
правки** (проверяют заголовки/роли/CTA-текст, не CSS-классы) — это и есть
подтверждение, что редизайн не задел функциональность.

**Проверка:** `npm run build`/`lint` зелёные, `npm run test:e2e` (23/23)
зелёные без изменений. Ad-hoc глазами — расширенная матрица: **3 страницы
(Пассажирам/Водителям/Бизнесу) × 2 вьюпорта (мобильный/десктоп) × 2 темы
(светлая/тёмная)**, восемь скриншотов плюс отдельная проверка hover-
состояния на десктопе (навёл на первую карточку перед снимком). Тёмная
тема была критичным пунктом матрицы — именно там подъём держится на
тональной разнице, а не на тени, нужно было убедиться глазами, что
карточки реально читаются приподнятыми, а не сливаются с фоном. Все
восемь комбинаций — карточки аккуратные, изумрудные чипы разборчивы в
обеих темах, CTA-кнопки (`DriversScreen`/`BusinessScreen`) сочетаются с
новым стилем без правок. Ноль console-ошибок во всех прогонах.

## 2026-07-29 — Багфикс: hover-эффект карточек не виден в тёмной теме

Eugene заметил сразу после мержа редизайна: «в темной теме вообще этого
ховера эффекта незаметно». Причина ровно та, что можно было предвидеть
(и не поймали скриншотами при проверке редизайна выше) — `box-shadow`
сам по себе плохой hover-сигнал на тёмном фоне: тень тёмная, фон тёмный,
контраста почти нет. `hover:shadow-md` из редизайна работал в светлой
(там подъём и держался в основном на тени), но не в тёмной.

**Фикс согласован с Eugene как «делай сейчас»** — без формального plan
mode на Opus, но с тем же уровнем эмпирической проверки: перед правкой
собрал автономный HTML-прототип (вне приложения, те же oklch-токены) с
пятью кандидатами hover-эффекта (shadow-only/фон-оверлей от
`--foreground`/бордер+оверлей/`bg-muted`/ring+оверлей+тень) и сравнил
скриншотами в тёмной, затем отдельно проверил лучший кандидат в светлой.
Выбор — **`hover:bg-muted`** (вместе с уже имевшимся `hover:shadow-md`):
- `--muted` — штатный токен проекта именно для мягких hover-фонов (см.
  `.claude/rules/design-tokens.md`, «нейтральный служебный токен для
  мягких hover/highlight-фонов») — используется ровно по назначению, не
  новый паттерн.
- В тёмной теме `--muted` (0.269) заметно светлее `--card` (0.205) —
  реальный, измеримый подъём. В светлой `--muted` (0.97) чуть темнее
  `--card`/`--background` (1.0) — тёплый лёгкий тон, тоже уместно.
- Не рамка/ring — сохраняет решение Eugene «чисто ховер без
  клик-аффорданса» (см. запись про редизайн выше) — фон, не контур,
  не создаёт ощущения кнопки.
- `transition-shadow` → `transition-all` (нужно анимировать и
  background-color, и box-shadow вместе).

**Методологический урок** (для себя на будущее): при проверке редизайна
карточек глазами по матрице 3×2×2 (см. запись выше) hover в тёмной теме
скриншотился и визуально казался нормальным — компрессия/масштаб
скриншота при рендере в диалоге маскировала недостаточный контраст тени.
При проверке ЭТОГО фикса тем же способом (screenshot до/после hover)
разница снова была на грани видимости на глаз — понадобилась **прямая
проверка через `getComputedStyle(el).backgroundColor`** (`before`:
`oklch(0.205 0 0)`, `after`: `oklch(0.269 0 0)`) вместо доверия
скриншоту. Для тонких hover/анимационных эффектов — предпочитать
computed-style assertions скриншотам как основной источник истины;
скриншоты — для общей композиции/layout, не для микроконтраста.

**Проверка:** `npm run build`/`lint`/`test:e2e` (23/23) зелёные,
изменение только в одном файле (`TopicPageScreen.tsx`, все три страницы
разом). Ad-hoc: пятивариантный прототип-сравнение (тёмная), отдельная
проверка лучшего варианта в светлой, финальная проверка через
computed-style на реальном приложении (dark, `Пассажирам`). Ноль
console-ошибок.

## 2026-07-29 — Редизайн десктоп-панели заказа: плавающая карточка (2d)

Eugene прислал два скрина (наш `taxi-project-tau.vercel.app` vs реальный
`taxi.yandex.ru`, десктоп, экран заказа): у Яндекса левая панель заказа —
приподнятая плавающая карточка над картой (тень, скругления, отступ от
края вьюпорта), внутри — визуально разделённые прямоугольные плашки
(адреса, классы, оплата, «Заказать»). У нас рельс был плоским
flush-сайдбаром (`border-r`), поля внутри — без группировки.

**Развилка (AskUserQuestion, до plan mode на Opus):**
- **Layout**: A (карточка парит над full-bleed картой, ближе к референсу,
  структурный риск для e2e-геометрии) vs B (рельс остаётся flush-сайдбаром,
  плашки только внутри, ниже риск). **Eugene выбрал A** — первый раз за
  четыре развилки подряд (три навбар-таба + эта) выбор пал на более
  сложный/рискованный вариант, а не безопасный — предыдущий паттерн
  «Eugene выбирает проще» не универсален, только эмпирическое наблюдение,
  не правило.
- **Скоуп**: без Промокода/Комментария водителя — остаются в бэклоге.

**Реализация:**
- `OrderScreen.tsx`: `<aside data-slot="order-rail">` — было
  `lg:w-[380px] lg:shrink-0 lg:border-r` (флекс-сосед карты, `order-first`
  для визуального переноса влево). Стало `lg:absolute lg:left-4 lg:top-4
  lg:bottom-4 lg:z-10 lg:rounded-2xl lg:border lg:bg-card lg:shadow-xl` —
  выведен из флекс-потока, `order-map-area` (`flex-1`) занимает всю
  ширину, `order-first` не нужен (это был трюк именно для флекс-раскладки).
  DOM-порядок (map-area физически первый) сохранён — не про визуальную
  позицию, а про порядок mount-эффектов (MapCanvas должен успеть
  выставить `mapRef.current` до эффекта `useDestinationSync` в рельсе).
  Full-height карточка (`top-4`+`bottom-4`, не auto-height по контенту) —
  осознанно: панели поздних фаз (`DriverSearchPanel`, `RideCompletionSheet`
  и т.п.) используют `h-full`/`mt-auto`, авто-высота их бы поломала.
  Следствие: пока видны только 2 поля адресов (до выбора Б), карточка
  визуально «пустая» снизу — принятый компромисс, не бага.
- `DESKTOP_PADDING` (камера при фите `[A,B]`): `left: 40 → 420` — карточка
  занимает 16px отступ + 380px ширины, без пересчёта фит прятал бы точку/
  маршрут под карточкой.
- `OrderComposePanel.tsx`: Откуда+Куда объединены в одну `bg-muted
  rounded-xl` плашку с тонким разделителем (`h-px bg-border`) между
  строками вместо двух независимых `border border-border` полей.
  Класс-грид секция лишилась `border-t border-border` разделителя —
  теперь просто следующий блок на поверхности карточки.
- `PaymentRow.tsx`: кнопка-триггер `border border-border` →
  `bg-muted rounded-xl` (тот же плашечный стиль). Дропдаун без изменений.
- **`ClassGrid.tsx` намеренно не тронут** — общий компонент с мобильным
  `ClassPickerSheet` (уже внутри своего elevated bottom-sheet), кнопки
  используют `hover:bg-muted`. Если положить класс-список на muted-плашку,
  ховер станет невидимым — тот же класс бага, что чинили пунктом выше
  (dark-theme hover). Список остаётся на поверхности карточки (`bg-card`),
  ховер контрастен в обеих темах.
- Токен-проверка перед реализацией (`src/index.css`): light `--card`=
  `--background`=1.0, `--muted`=0.97 (плашка чуть темнее — читается);
  dark `--background`=0.145, `--card`=0.205, `--muted`=0.269 (плашка
  светлее карточки — читается). Подтверждено на реальном приложении через
  `getComputedStyle` (см. «Проверка» ниже), не только по значениям токенов.
- `e2e/desktop-layout.spec.ts`: гео-ассерт `mapAreaBox.x >= railBox.x +
  railBox.width` (флеш-примыкание) переписан под намеренный оверлей —
  карта начинается на/до левого края карточки и продолжается за её правым
  краем, карточка отступает от обоих краёв вьюпорта (`railBox.x > 0`,
  `railBox.y > 0`).

**Проверка:** `npm run build`/`lint`/`test:e2e` (23/23, включая
переписанный гео-ассерт) зелёные. Ad-hoc глазами: десктоп светлая (пустая
+ заполненная панель), десктоп тёмная (через реальный UI-тумблер
Профиль→Настройки, не `classList` напрямую), мобильный регресс
(bottom-sheets не задеты). Computed-style проверка на тёмной теме
(`getComputedStyle` для `order-rail`/адрес-плашки/payment-плашки) —
`0.205` (карточка) vs `0.269` (плашки), подтверждает разделение до
скриншота. Ноль console-ошибок во всех прогонах.

## 2026-07-29 — Редизайн панели заказа v2: несколько независимых карточек

Первая итерация (запись выше) не совпала с эталоном — Eugene прислал
реальный скрин `taxi.yandex.ru`, и выяснилось: это **не одна карточка-рельс
с плашками внутри, а несколько НЕЗАВИСИМЫХ плавающих карточек** в столбик
с зазорами (в зазорах видна карта), каждая по высоте своего контента:
адреса, классы (с ценами «от X ₽», видны СРАЗУ, до ввода «Куда?»), оплата,
кнопка «Заказать» отдельным блоком. Ошибка первой итерации — сравнение шло
по текстовому описанию скрина из памяти прошлой сессии, не по самому
скрину (изображение не сохранялось файлом, было вложением в чате прошлой
сессии — см. `docs/roadmap.md`/decision-запись выше). Урок: при «сделай
как на скрине» без самого файла — переспрашивать/просить скрин ещё раз
перед реализацией, не полагаться на пересказ.

**Развилка с Eugene**: без Промокода/Комментария/Остановки (эталон их
показывает, но Eugene явно подтвердил не добавлять). Eugene сам
сформулировал ключевое следствие: «логику придётся поменять» — классы и
оплата у Яндекса видны ДО ввода точки Б, у нас были скрыты за
`destination &&`.

**Архитектурная находка**: все десктоп-панели поздних фаз
(`DriverSearchPanel`/`RideCompletionSheet`/`RideDoneCard` через
`OrderSurface`, `DriverCard`) рендерились на десктопе голыми плоскими
блоками — карточкой был сам рельс. Для «нескольких карточек» пришлось
спустить карточную обёртку с рельса в каждую поверхность.

**Реализация:**
- Новый примитив `src/shared/ui/OrderCard.tsx` — `rounded-2xl border
  bg-card p-4 shadow-lg`, с опциональным `data-slot` (по умолчанию
  `order-card`, переопределяется когда нужен свой e2e-слот, как у
  `DriverCard`). Единый источник карточного хрома.
- `OrderSurface.tsx` (десктоп-ветка) и `DriverCard.tsx` (десктоп-ветка) —
  обёрнуты в `OrderCard` вместо голого `<div className="p-4">`/
  `flex flex-col gap-3 p-4`. Мобильные ветки не тронуты.
- `OrderScreen.tsx`: `<aside data-slot="order-rail">` — убрана карточная
  хром-обёртка (`bg-card`/`shadow-xl`/`border`), рельс теперь прозрачный
  `lg:flex lg:flex-col lg:gap-3` контейнер-позиционер. Карта видна в
  зазорах между карточками (как у Яндекса).
- `OrderComposePanel.tsx`: корневой `<div>` заменён на React-фрагмент из
  трёх `OrderCard` (адреса с `data-slot="order-compose"` — e2e-слот
  сохранён; классы; оплата) + отдельная кнопка «Заказать» вне карточек.
  Гейт `destination &&` заменён на `pickup &&` для классов/оплаты/кнопки.
  Кнопка без destination — не disabled, кликом фокусит инпут «Куда»
  (`destinationInputRef`) вместо отправки `CONFIRM_CLASS`; с destination —
  прежнее поведение (disabled пока нет `selectedQuote`).
- `entities/ride-class/api.ts`+`mocks.ts`: `destination` в
  `RideClassQuotesArgs` стал опциональным; без него мок считает
  `distanceKm=0` → цена = `baseFare` (99/149/299 ₽) — то самое «от X ₽».
- `ClassGrid.tsx`: новый проп `estimate?: boolean` — префикс «от» перед
  ценой. Мобильный `ClassPickerSheet` не передаёт (всегда есть B, точная
  цена) — поведение не изменилось.
- `PaymentRow.tsx`: убрана собственная `bg-muted`-обёртка кнопки-триггера
  (была нужна в v1, когда рельс сам не был карточкой) — теперь плоская,
  карточность даёт внешний `OrderCard`.

**Баг, найденный при первом прогоне e2e (не по плану, диагностирован и
починен по ходу)**: `SELECT_CLASS` в `machine.ts` был обработчиком только
внутри состояния `selectingClass`. Классы теперь показываются (и
авто-выбираются эффектом в `OrderComposePanel`) ещё в `selectingPickup`/
`selectingDestination` — XState молча роняет события без обработчика в
текущем состоянии, так что `selectedClassId` никогда не устанавливался,
пока не была введена точка Б. Симптом: e2e видел кнопки классов с ценами,
но ни одна не имела `aria-pressed="true"`. Фикс — `SELECT_CLASS` поднят в
глобальный `on:` блок машины (рядом с `SET_PICKUP`, который уже был
глобальным по той же причине — доступен из любого состояния), дублирующий
обработчик в `selectingClass` убран. Безопасно: событие может отправить
только `OrderComposePanel`, который сам не рендерится вне
pickup/destination/class-фаз.

**Проверка:** `npm run build`/`lint`/`test:e2e` (23/23) зелёные —
`desktop-layout.spec.ts` дополнен проверкой, что классы/оплата/кнопка
видны и ценник несёт префикс «от» ДО ввода Б, и что префикс исчезает
после. Ad-hoc глазами: десктоп светлая (до/после Б, поиск водителя —
каждая фаза своя карточка), десктоп тёмная (реальный тумблер темы,
`getComputedStyle` на всех карточках рельса подтвердил `oklch(0.205 0 0)`
— совпадает с `--card`, отличимо от чёрной карты), мобильный регресс без
изменений. Ноль console-ошибок во всех прогонах.

## 2026-07-30 — Мобильный ручной ввод точки А (тап по плашке «Точка подачи»)

Eugene: на мобильном экране A плашка «Точка подачи» сверху была
неинтерактивным `<div>` — задать A можно было только перетаскиванием
карты. Запрос — тап по плашке открывает ручной ввод адреса подачи, точка
ставится куда выбрали, маркер по-прежнему перетаскивается для точной
подстройки.

Паттерн уже был на десктопе — `OrderComposePanel.tsx`'s `isEditingPickup`
превращает поле «Откуда» в поисковый инпут с выпадашкой; перенесли тот же
подход в мобильную плашку `PickupSheet.tsx` (данные — `useSearchPlacesQuery`
+ `useDebouncedValue`, реюз `PlaceRow`), ничего не изобретая заново.

**Реализация:**
- Плашка `data-slot="pickup-pill"` — `<div>` → `<button aria-label="Изменить
  точку подачи">`, тап включает `isEditingPickup`. В режиме редактирования
  та же позиция (`absolute left-20 right-4 top-4`) занята карточкой с
  инпутом (`data-slot="pickup-address-input"`, `autoFocus`) и выпадашкой
  результатов (`data-slot="pickup-address-results"`, `onMouseDown`
  `preventDefault` — тот же приём, что в `OrderComposePanel`, чтобы клик по
  результату не терялся из-за blur раньше onClick).
- Выбор результата — `handlePickPickupResult`: `mapRef.jumpTo(...)` +
  `SET_PICKUP` (**без** `CONFIRM_PICKUP`) — в отличие от
  `handlePickRecent`, остаёмся на экране A, не уходим к выбору Б.
  `jumpTo` не поднимает `dragstart` (см. комментарий в файле про гонку с
  Москвой), так что существующий `userDraggedRef`-гейт на `moveend` не
  триггерится повторно — `SET_PICKUP` шлём явно один раз, drag после
  выбора работает как тонкая подстройка от нового центра.
- `aria-label` на плашке — иначе accessible name кнопки включил бы текст
  адреса («…Тверская…») и столкнулся бы с `getByRole('button',
  {name:/Тверская/})` в двух существующих e2e-тестах (целятся в «недавний»
  адрес) → strict-mode падёж на двух совпадениях. `toContainText`
  (по тексту, не по role) продолжает работать без изменений.
- Машину (`machine.ts`) не трогали — `SET_PICKUP` уже был глобальным
  обработчиком.

**Проверка:** `npm run build`/`lint`/`test:e2e` (24/24, новый тест —
тап → ввод → выбор реального адреса из e2e-мока → остаёмся на A → drag
после выбора всё ещё двигает точку) зелёные. Ad-hoc глазами (реальный
MapTiler, мобильный вьюпорт): тап → автофокус → выпадашка результатов →
выбор прыгает картой и возвращает обычную плашку с новым адресом, экран
остаётся A; drag после выбора работает (подтверждено программно, не
только на глаз); пустой blur возвращает обычную плашку; тёмная тема
(реальный тумблер) — инпут и результаты читаемы. Десктоп не затронут —
`PickupSheet` mobile-only, `desktop-layout.spec.ts` прошёл без изменений.
Ноль console-ошибок.

## 2026-07-30 — Багфикс: неверная цена классов при сбое запроса на десктопе

Eugene прислал скрин: далёкий заказ Ростов→Москва, под классами текст
«Не удалось загрузить классы. Попробуйте ещё раз.», но при этом сами
классы показывали 99/149/299 ₽ — ровно `baseFare` без расстояния, хотя
реальная поездка на ~1100 км должна стоить на порядок дороже. Выглядело
как «и ошибка, и (неправильная) цена» одновременно.

**Причина — не сеть/OSRM/дальность, а как читали состояние RTK Query.**
`useGetRideClassQuotesQuery` в `OrderComposePanel.tsx` перезапрашивается с
новым ключом кэша (`{pickup, destination}`) при появлении Б. Поле `data` в
RTK Query **намеренно** держит данные от ПРЕДЫДУЩИХ аргументов, пока новый
запрос не завершится (документировано в типах: «tries to hold data as
much as possible, also reusing data from the last arguments») — так
задумано, чтобы не мигал UI при смене аргументов. Но если НОВЫЙ запрос (с
destination) падает, `isError` становится `true`, а `data` остаётся
старым — тем самым `{pickup}`-only «от X ₽» estimate-результатом с прошлого
шага. Компонент показывал и текст ошибки, и эти устаревшие (заниженные)
числа как будто они актуальны.

Первопричину самого сетевого сбоя воспроизвести стабильно не удалось (два
прогона на живом деплое по тем же шагам прошли без ошибок — видимо,
разовая сетевая помеха на стороне Eugene), но сам механизм показа неверной
цены при ЛЮБОМ таком сбое — реальный, воспроизводимый баг, не совпадение.

**Фикс (по согласованию с Eugene — узкая, полностью продиагностированная
правка, без формального plan mode, тот же прецедент, что hover-баг
2026-07-29):**
- `selectedQuote` больше не читает `quotes` при `isQuotesError` —
  `!isQuotesError ? quotes?.find(...) : undefined`.
- Класс-карточка при ошибке не рендерит `ClassGrid` (со старыми `quotes`)
  вообще — вместо неё текст ошибки + кнопка «Повторить» (`refetchQuotes()`
  из хука), тот же паттерн, что уже был в `DriverSearchPanel` для отказа
  поиска водителя.
- Кнопка «Заказать» из-за `selectedQuote===undefined` в состоянии ошибки
  показывает просто «Заказать» (без цены) и остаётся disabled — не даёт
  оформить заказ по неверной цене.

**Проверка:** временно (не закоммичено) заставил мок `/api/ride-classes`
падать на запросах с `destLat`, чтобы гарантированно воспроизвести именно
этот сценарий на dev-сервере — экран стал показывать ровно то же самое
(маршрут Ростов→Москва), но вместо 99/149/299 ₽ корректно: ошибка +
«Повторить», без единого числа старой цены на экране. После проверки мок
вернул в исходное состояние (`git diff` — пусто, файл не менялся).
`npm run build`/`lint`/`test:e2e` (24/24) зелёные без правок тестов — баг
специфичен для error-состояния запроса, которое не воспроизводится под
детерминированными e2e-моками (не было отдельного теста и раньше для этой
ветки, симметрично `DriverSearchPanel`, где 20%-й отказ покрыт ретраем
в уже существующем тесте, а не новым).

## 2026-07-30 — Крестик-очистка точки Б (десктоп + мобилка)

Eugene прислал скрин десктопной панели заказа (после редизайна 2d) и
попросил добавить крестик-очистку в поле «Куда», явно попросив продумать
логику, а не просто стереть текст.

**Почему это не тривиальная кнопка**: маркер Б, линия маршрута, котировки
классов — всё в `OrderScreen`/`OrderComposePanel` завязано на
`context.destination` машины, а не на текст инпута. Очистка одного текста
оставила бы маршрут/маркер/цены висеть рассинхронизированно с пустым
полем. Значит крестик обязан обнулять `destination` в контексте, не только
локальный `query`.

**Развилка (AskUserQuestion)**: делать крестик только в десктопной
`OrderComposePanel` или ещё и в мобильном `DestinationSheet` — **выбрали
оба вьюпорта**.

**Асимметрия вьюпортов, найденная чтением кода `useDestinationSync`**:
- **Десктоп** (`OrderComposePanel`): `onPick`/`onUserSettle` коммитят
  `SET_DESTINATION` сразу при выборе результата/settle драга — `destination`
  в контексте реально установлен, пока Б не очищена явно. Крестик здесь
  делает реальную работу.
- **Мобилка** (`DestinationSheet`): `useDestinationSync` вызывается БЕЗ
  `onPick`/`onUserSettle` — `destination` попадает в контекст только через
  `handleConfirm` (кнопка «Подтвердить точку назначения»). На экране Б
  `destination` всегда `null`, пока не подтверждено — крестик там по факту
  чистит только текст/поиск. `CLEAR_DESTINATION` всё равно шлётся (для
  единообразия события между вьюпортами), просто безвредный no-op по
  контексту.

**Реализация:**
- `types.ts` + `machine.ts`: новое событие `CLEAR_DESTINATION`, НЕ
  глобальное (в отличие от `SET_PICKUP`/`SELECT_CLASS`) — обработано
  только в `selectingDestination` (self-target, сброс `destination` +
  `selectedClassId`) и `selectingClass` (target → `selectingDestination`,
  явный возврат к выбору Б, а не полагание на то, что `always`-гейт
  `hasDestination` сам перестанет матчить). Не путать с
  `BACK_TO_DESTINATION` — тот сохраняет Б, этот обнуляет.
- `OrderComposePanel.tsx`/`DestinationSheet.tsx`: крестик (`X` из
  lucide-react) в конце поля «Куда», видим при
  `query.length > 0 || Boolean(destination)`. `onMouseDown`
  `preventDefault` — тот же приём, что у выпадашки результатов, чтобы клик
  не снял фокус раньше `onClick`. По клику — `setQuery('')` +
  `CLEAR_DESTINATION`.
- Всё остальное (маркер/маршрут/оценка цены/кнопка «Заказать») каскадом
  само откатывается от обнуления `destination` — правок в `OrderScreen`
  не потребовалось.

**e2e-грабля**: тест сначала выбирал Б, чистил крестиком, затем пытался
выбрать Б заново тем же `.fill()` на инпуте — таймаут на поиске результата
в выпадашке. Причина — после первого выбора реальный DOM-фокус на инпуте
сохраняется (тот же трюк `onMouseDown preventDefault`, что держит фокус
во время клика по строке результата), а React-стейт `isDestinationFocused`
явно сбрасывается в `false` (`handlePickDestinationRow`), чтобы закрыть
выпадашку. Playwright `.fill()` не порождает новое DOM `focus`-событие на
уже сфокусированном элементе — `isDestinationFocused` не возвращался в
`true`, выпадашка не открывалась. Фикс — не баг приложения, баг теста:
клик по нейтральной области карты (принудительный blur) перед повторным
кликом+`.fill()` на инпуте.

**Проверка:** `npm run build`/`lint`/`test:e2e` (25/25, два новых теста —
десктоп: крестик скрыт до Б → выбор Б → крестик → маршрут/маркер/цены
откатились (source `route-line` → `coordinates.length === 0`, не `null` —
`applyRouteLine` пишет пустой `LineString`, не удаляет source) → повторный
выбор Б работает; мобилка: крестик скрыт без текста → появляется с текстом
→ чистит поле, лист/кнопка подтверждения на месте) зелёные. Ad-hoc
глазами: десктоп (маркер+маршрут+точные цены → крестик → всё пусто,
цены снова «от X ₽», кнопка «Заказать» без цены), тёмная тема (крестик
виден/контрастен), мобилка (текст → крестик → пусто, список адресов и
карта не задеты). Ноль console-ошибок во всех прогонах.

## 2026-07-30 — Багфикс: страница зумится как веб-сайт вместо «app-like»

**Баг (Eugene, живое демо на iPhone):** тап на плашку «Точка подачи» →
фокус на инпуте ручного ввода адреса → у Eugene «приблизился» весь экран
(скрин приложен в чате). Плюс отдельный запрос — чтобы зумить (pinch/
double-tap) можно было только карту, а не всю страницу, как в нативном
приложении.

**Развилка, решённая сразу (без AskUserQuestion, очевидно из формулировки
Eugene):** второй запрос — не отдельная фича, а тот же баг-фикс. Оба
симптома — частные случаи одной причины: страница ведёт себя как обычный
веб-сайт (зумится целиком), а не как «упакованное» приложение.

**Три независимых механизма зума страницы, три независимых фикса** (важная
поправка к тому, что казалось очевидным в чате: одного `<meta viewport
maximum-scale=1, user-scalable=no>` НЕ хватает — с iOS 10 Safari намеренно
игнорирует эти параметры в обычной вкладке из соображений доступности;
работает только на Android/десктопе):

1. **Авто-зум при фокусе на инпуте (сам баг со скрина)** — iOS Safari
   зумит страницу к инпуту, если у него `font-size < 16px`. Реальная
   причина: `data-slot="pickup-address-input"` и `data-slot="destination-
   input"` были на `text-sm` (14px). Фикс — `text-sm` → `text-base` (16px)
   на всех мобильных текстовых инпутах, которые реально фокусятся (те два
   + три инпута формы карты в `PaymentMethodsScreen`). Десктопные инпуты
   `OrderComposePanel` не трогали — там авто-зума нет.
2. **`<meta viewport>` (`index.html`)** — добавлен `maximum-scale=1.0,
   user-scalable=no`. Работает на Android Chrome и в установленном
   standalone-PWA; на iOS Safari в обычной вкладке — no-op, но безвредный
   defense-in-depth слой (не мешает п.1/3).
3. **`touch-action: manipulation` на `body`** (`index.css`, `@layer base`).
   Разобрались через web-search (см. ниже) — вопреки стандартной семантике
   CSS Touch Action Level 2 (где `manipulation` = разрешить pan + pinch,
   запретить только double-tap), **в iOS Safari `pinch-zoom` как отдельное
   значение не реализован вовсе** — Safari поддерживает только `auto` и
   `manipulation`/`none`/`pan-*`, и в этой урезанной модели `manipulation`
   фактически блокирует и double-tap, и pinch-zoom страницы. Источники:
   [paulau.dev](https://paulau.dev/blog/disable-pinch-zoom-on-ios-safari/),
   независимое подтверждение через второй web-search (WebKit не
   реализовал отдельный `pinch-zoom`-keyword, тикет webkit.org/b/133112).

**Отказались от исходно запланированного 4-го слоя** (JS `preventDefault`
на `document`-событиях `gesturestart`/`gesturechange`/`gestureend`,
Apple-специфичный способ гасить page-level pinch на iOS) — план изначально
включал его, но web-search в процессе реализации показал, что `touch-
action: manipulation` уже закрывает pinch-zoom в текущем iOS Safari без
JS. Добавлять его поверх было бы неиспользуемым defensive-кодом (см.
CLAUDE.md — не писать код под сценарии, которые не происходят). Если
живая проверка на телефоне Eugene покажет, что pinch страницы всё же
где-то проскакивает — вернуться к этому слою отдельным пунктом, не сейчас.

**Почему карта (MapLibre) не пострадала:** `touch-action` не
наследуется по CSS-каскаду — эффективное значение для тачей, начавшихся
НА конкретном элементе, берётся из его собственного `touch-action`, не
из `body`. MapLibre сам ставит `touch-action: none` на свой canvas
(подтверждено `getComputedStyle` в ad-hoc-проверке) и обрабатывает pinch
через сырые `touchstart`/`touchmove`, а не через iOS `gesture*`-события —
ни один из трёх слоёв её не задевает.

**Проверка:** `npm run build`/`lint`/`test:e2e` — 25/25 зелёные (ни один
тест не завязан на touch-action/viewport-механику, регрессий нет). Ad-hoc
Playwright с мобильной эмуляцией (`devices['iPhone 13']`): `viewport`
meta и `body` touch-action — ожидаемые значения; `pickup-address-input`/
`destination-input` — `16px`; `visualViewport.scale` после фокуса — `1`
(без скачка); канвас карты — `touch-action: none` не тронут, программный
`zoomTo` по-прежнему меняет `getZoom()`; ноль console/page-ошибок.
**Честное ограничение:** Chromium не воспроизводит iOS-специфичные пути
(авто-зум < 16px и `gesture*`-события — Apple-only), так что pinch/авто-
зум по-настоящему протестировать локально нельзя — финальное
подтверждение просил у Eugene на его iPhone (том самом со скрина) после
мержа.

## 2026-07-30 — Багфикс: панель на экране Б не подныривает при первом драге (iOS)

**Баг (Eugene, живое демо на iPhone):** ожидаемое поведение — во время
переноса карты на экране Б нижняя панель уезжает вниз (не до конца, видна
полоска), при остановке возвращается. Реально срабатывало **только после
того**, как пользователь уже выбрал адрес из списка (программный `jumpTo`
камеры) — самый первый живой драг панель игнорировала.

**Диагностика — сначала пытались воспроизвести, не гадать:**
- В Chromium (Blink) — и мышью, и синтетическим тачем через CDP
  (`Input.dispatchTouchEvent`) — баг **не воспроизводится ни в одном
  сценарии**: оба пути входа на экран Б, немедленный драг и с задержкой,
  до и после выбора адреса — везде `dragstart` летит с `originalEvent`
  сразу же, панель подныривает корректно. Это опровергло исходную гипотезу
  из роадмапа (гонка монтирования `mapRef` — эффект подписки на map-
  события якобы не успевает навеситься) — слушатели навешаны и получают
  событие с первого раза.
- Поставили Playwright WebKit (движок Safari) для честной проверки — но
  синтетический тач в нём недоступен (`Touch`-конструктор и
  `initTouchEvent` не реализованы в этой версии), так что напрямую
  воспроизвести iOS-путь локально не вышло.
- Уточнили у Eugene на реальном iPhone: во время бага **карта видимо
  двигается под пальцем, панель — нет** (жест долетает до карты, значит
  дело не в перехвате тача чем-то ещё); баг одинаков **и в PWA с
  домашнего экрана, и в обычной вкладке Safari** (исключает адресную
  строку iOS и специфику PWA-контейнера — остаётся сам движок).

**Диагноз:** триггер ретрита подписан на MapLibre-событие `dragstart` и
гейтит по `e.originalEvent` (`useDestinationSync.ts`,
`handleUserMoveStart`). На iOS Safari самый первый пользовательский
тач-пан визуально двигает карту, но MapLibre не эмитит `dragstart` с
`originalEvent` до тех пор, пока не случилось хотя бы одно **программное**
перемещение камеры (`jumpTo`) — оно «прогревает» drag-эмиссию библиотеки
на этом движке. В Blink такого прогрева не требуется — там `dragstart`
эмитится с первого же пана, поэтому баг воспроизводим только на реальном
Safari/iOS.

**Фикс:** в `useDestinationSync.ts`, в том же эффекте, где вешаются
`dragstart`/`zoomstart`/`moveend`, добавлен **no-op `jumpTo` в текущий
центр карты** (`map.jumpTo({ center: map.getCenter() })`) сразу при
монтировании — ровно то действие, которое, по наблюдению Eugene, чинило
ретрит при выборе из списка, только теперь автоматически и до первого
реального драга. Визуально ничего не сдвигается (center не меняется);
программный `moveend` от этого вызова штатно отсекается существующим
гейтом `isUserDrivenMoveRef.current` в `handleMoveEnd` (как и раньше для
любых других программных перемещений).

**e2e-грабля, найденная при написании регресс-теста:** новый тест
(`pickup-destination.spec.ts`, драг сразу после входа через «Куда едем?»,
без предварительного выбора — именно тот путь, что был сломан) сперва
падал на возврате `sheet` к rest-позиции после отпускания мыши. Причина —
не баг фикса, а гонка в самом тесте: `DestinationSheet` монтируется с
`sheetHeight` = 0 (`useState(0)`), поэтому `peekY = 0` и шторка на первом
рендере стоит на `translateY(0)`; только после `useEffect`, измеряющего
реальную высоту контейнера, `peekY` пересчитывается и шторка спринг-
анимацией уезжает в настоящую rest-позицию. Тест мерил `restTop` сразу
после `toBeVisible()`, поймав переходную до-спринговую позицию — драг
затем корректно возвращал шторку в НАСТОЯЩИЙ rest, но тест сравнивал с
неверным (транзиентным) baseline и падал. Подтверждено ad-hoc-скриптом с
логом `dragstart`/`dragend`/`moveend` и опросом `map.isMoving()`/
`boundingBox()` каждые 300мс — итоговая позиция стабильна и совпадает с
ожидаемой `peek`-высотой, `isMoving() === false`. Фикс теста —
`page.waitForTimeout(500)` после появления шита, перед тем как снимать
baseline (тот же паттерн задержки уже использовался в более ранних ad-hoc
проверках этого экрана).

**Проверка:** `npm run build`/`lint`/`test:e2e` — 26/26 зелёные, новый
тест ловит именно тот путь входа, что был сломан (через «Куда едем?», без
предварительного `jumpTo`). **Настоящее доказательство фикса — только на
реальном iPhone** (Chromium/WebKit-CI этот баг никогда не ловили и не
поймают) — Eugene согласился на разовое отклонение от «фича-ветки только
локально»: смерджено сразу в `main`, попросили подтвердить на устройстве
после деплоя; если не поможет — запасной вариант (не реализован, см. план
`bug-sheet-retreat-ios.md` в момент фикса) — перевести триггер ретрита с
MapLibre `dragstart` на нативные `touchstart`/`touchmove` прямо на
контейнере карты, независимо от того, как MapLibre классифицирует жест.
