# Roadmap

Фича за фичей, план меняется по ходу — не хранить в CLAUDE.md. Порядок —
рабочая гипотеза, пересматривать при новых вводных от Eugene/Миши.

Каждый пункт — отдельная `feature/<slug>` ветка, план-mode перед стартом,
явный аппрув перед мёржем в `main` (см. `.claude/rules/git-workflow.md`).

## Готово

Подробности (что сделано, какие файлы, как проверено в браузере) —
[`_archive/roadmap-done.md`](_archive/roadmap-done.md). Здесь только список
для навигации; «почему» тех или иных решений — `decisions.md`.

- [x] Каркас проекта + дизайн-токены
- [x] Машина состояний заказа (XState) + карта (MapLibre)
- [x] Реальный MapTiler style + rAF-трекинг маркера водителя
- [x] Класс такси / выбор точки подачи (drag + bottom-sheet + MSW-цена)
- [x] Поиск машины / назначение водителя
- [x] Поездка в реальном времени (авто-статусы, таймеры)
- [x] Оплата / оценка поездки
- [x] Профиль пользователя + навигация (второй экран)
- [x] История поездок (третий экран)
- [x] PWA-полировка (манифест, иконки, offline)
- [x] Реальная геолокация + модель «Я»≠A
- [x] Геолокация сразу при входе (полная модель Яндекса)
- [x] Такси едет по дорогам, не напрямую (OSRM)
- [x] Bottom sheet выбора точки Б в стиле Яндекса (поиск адреса, MapTiler Geocoding)
- [x] Камера следит за едущим такси (auto-fit + override/автовозврат)
- [x] Убран стартовый экран «Начать заказ» (полная модель Яндекса, точка А сразу перемещаемая)

## Дальше

1. **Как Миша будет смотреть демо** — самый последний пункт, после всех
   правок дизайна (от Eugene/Миши). Решить: локальный запуск у него на
   компе (`npm run dev`/`preview`) или залить на бесплатный хостинг, чтобы
   зайти прямо с телефона и потрогать вживую (mobile-first — с телефона
   нагляднее). **Ключевой нюанс:** данные живут на MSW, а MSW гейтится на
   dev (`import.meta.env.PROD` в `main.tsx`) — голая прод-сборка на
   статик-хостинге отдаст красивую устанавливаемую оболочку, но с мёртвыми
   запросами. Варианты: (а) хостить dev/preview; (б) снять dev-гейт с MSW
   специально для демо-сборки (тогда любой статик-хостинг работает целиком,
   как задумано «нет бэкенда = MSW имитирует»). Здесь же — финальная
   проверка PWA-установки с реального телефона. Ещё нюанс: геолокация
   требует secure context (https) — localhost и любой нормальный
   статик-хостинг (Netlify/Vercel/GH Pages дают https) подходят, но по
   http с телефона геолокация молча уйдёт в fallback `DEMO_PICKUP`.

## Открытое

Пусто — «Улучшенный bottom-sheet (drag-to-snap физика)» закрыт вместе с
`DestinationSheet` (см. «Готово»).
