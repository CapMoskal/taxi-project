---
description: Подготовка перед /compact — обновить файлы знаний, доки, проверить git-состояние.
---

Eugene собирается запустить `/compact`. Перед этим приведи в порядок всё,
что иначе потеряется вместе с контекстом. Ничего не коммить, не пушить и не
мержить без явного отдельного запроса — только собери/запиши состояние.
Пройди по пунктам, для каждого — короткое решение "нужно/не нужно", не
разворачивай в эссе.

## 1. Память (`/Users/evgenimoskalev/.claude/projects/-Users-evgenimoskalev-Desktop-taxi-project/memory/`)

Прочитай `MEMORY.md` и вспомни ход текущей сессии. Для каждого типа памяти —
реально ли за сессию появилось что-то, что стоит сохранить (по правилам из
системного промпта: user/feedback/project/reference, включая исключения —
не дублировать то, что и так живёт в коде/git/docs):

- **feedback** — были ли явные поправки ("не делай так") или подтверждения
  ("да, именно так") от Eugene по поводу подхода к работе?
- **project** — изменились ли текущие цели/дедлайны/статусы фич, которые не
  выводятся тривиально из git log?
- **user** — узнал ли что-то новое о роли/предпочтениях Eugene?
- **reference** — появились ли новые внешние ссылки/источники правды?

Обнови существующие файлы (не плоди дубликаты — сначала проверь, нет ли уже
подходящего файла) или создай новые. Обнови `MEMORY.md`, если у изменённого
файла поменялся смысл настолько, что однострочник в индексе стал неточным.
Проверь `[[ссылки]]` между файлами на битость (кроме намеренно висящих).

## 2. Проектная документация (git-tracked, отдельно от памяти)

- `docs/decisions.md` — есть ли за сессию архитектурное решение (новое
  событие машины, смена паттерна, нетривиальный баг-фикс с причиной), которое
  ещё не записано ADR-записью? Если да — добавить.
- `docs/roadmap.md` — есть ли завершённые за сессию пункты, которых ещё нет
  в разделе «Готово»? Добавить короткой строкой.

## 3. Git-состояние

`git status` + `git log --oneline -5`. Явно перечисли Eugene:
- есть ли незакоммиченные изменения;
- есть ли локальные коммиты, не запушенные в `main`;
- есть ли недомерженная feature-ветка.

Не исправляй это сам — просто доложи, чтобы ничего не потерялось при
компакте.

## 4. Открытые задачи

Если в сессии осталось что-то начатое, но не доведённое до конца (фича
наполовину, тест не позеленел, план одобрен но не реализован) — явно
назови, чтобы после компакта не потерялось из виду.

## Итог

Заверши одним коротким сообщением Eugene: что обновил, что нашёл в п.3/4
(если есть что-то незавершённое) и что готов к `/compact`. Без лишнего
буллит-списка на весь ответ — только по существу.
